{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "//#syncpack:list": {
      "outputs": []
    },
    "//#lint": {
      "dependsOn": ["@ttoss/config#build"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["//#syncpack:list", "^build"],
      "outputs": ["build/**", "dist/**", ".next/**", "storybook-static/**"]
    },
    "test": {
      "dependsOn": ["//#syncpack:list", "^build"],
      "outputs": [],
      "inputs": [
        "src/**/*.tsx",
        "src/**/*.ts",
        "tests/**/*.ts",
        "tests/**/*.tsx"
      ]
    },
    "deploy": {
      "dependsOn": ["//#lint", "build", "test"],
      "outputs": [".carlin/**"]
    }
  }
}
