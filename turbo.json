{
  "$schema": "https://turborepo.org/schema.json",
  "globalEnv": [
    "AWS_*",
    "CARLIN_*",
    "ENVIRONMENT",
    "NODE_ENV",
    "TTOSS_MONOREPO"
  ],
  "tasks": {
    "build:config": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "build-config": {
      "dependsOn": ["build:config"],
      "outputs": ["dist/**"]
    },
    "i18n": {
      "dependsOn": ["^build-config", "^i18n"],
      "outputs": ["i18n/lang/**"]
    },
    "build": {
      "dependsOn": ["build-config", "i18n", "^build"],
      "outputs": ["dist/**", "build/**", ".next/**", "storybook-static/**"]
    },
    "test": {
      "dependsOn": ["i18n"],
      "outputs": [],
      "inputs": [
        "src/**/*.tsx",
        "src/**/*.ts",
        "tests/**/*.ts",
        "tests/**/*.tsx"
      ]
    },
    "deploy": {
      "dependsOn": ["i18n", "build", "carlin#build", "^deploy"],
      "outputs": [".carlin/**"]
    }
  }
}
