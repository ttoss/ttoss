{
  "$schema": "https://turborepo.org/schema.json",
  "globalEnv": ["CARLIN_ENVIRONMENT"],
  "pipeline": {
    "//#build:config": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "//#lint": {
      "dependsOn": ["//#build:config"],
      "outputs": []
    },
    "//#syncpack:list": {
      "dependsOn": ["//#build:config"],
      "outputs": []
    },
    "i18n": {
      "dependsOn": ["//#lint", "^build"],
      "outputs": ["i18n/lang/**"]
    },
    "build": {
      "dependsOn": ["//#lint", "//#syncpack:list", "^build", "i18n"],
      "outputs": ["build/**", "dist/**", ".next/**", "storybook-static/**"]
    },
    "test": {
      "dependsOn": ["i18n"],
      "outputs": [],
      "inputs": [
        "src/**/*.tsx",
        "src/**/*.ts",
        "tests/**/*.ts",
        "tests/**/*.tsx"
      ]
    },
    "deploy": {
      "dependsOn": ["i18n", "carlin#build", "build", "test", "^deploy"],
      "outputs": [".carlin/**"]
    }
  }
}
