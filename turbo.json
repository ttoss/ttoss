{
  "$schema": "https://turborepo.org/schema.json",
  "globalEnv": ["CARLIN_ENVIRONMENT", "ENVIRONMENT"],
  "pipeline": {
    "//#build:configs": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "//#lint": {
      "dependsOn": ["//#build:configs"],
      "outputs": []
    },
    "//#syncpack:list": {
      "dependsOn": ["//#build:configs"],
      "outputs": []
    },
    "topo": {
      "dependsOn": ["//#build:configs", "//#lint", "//#syncpack:list"],
      "outputs": []
    },
    "i18n": {
      "dependsOn": ["topo"],
      "outputs": ["i18n/lang/**"]
    },
    "build": {
      "dependsOn": ["topo", "i18n"],
      "outputs": ["dist/**", "build/**", ".next/**", "storybook-static/**"]
    },
    "test": {
      "dependsOn": ["topo", "i18n"],
      "outputs": [],
      "inputs": [
        "src/**/*.tsx",
        "src/**/*.ts",
        "tests/**/*.ts",
        "tests/**/*.tsx"
      ]
    },
    "deploy": {
      "dependsOn": [
        "topo",
        "i18n",
        "build",
        "test",
        "carlin#build",
        "carlin#test",
        "^deploy"
      ],
      "outputs": [".carlin/**"]
    }
  }
}
