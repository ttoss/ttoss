# Friction log — FSL Studio (P1 adoption)

> The Studio evidence channel — Program P1 and every `BLUEPRINT.md` slice
> after it (see `packages/fsl-ui/INTERNAL/ROADMAP.md` §Program). Every
> hand-rolled style, missing component, confusing API, or `llms.txt`/CONTRACT
> gap found while building the Studio is filed here.
> **This log is the fsl-ui v1 backlog.** Doc gaps are fixed immediately;
> package gaps stay open until shipped. One entry per finding — append only.

Severity: `blocker` (cannot express the flow inside the system) ·
`gap` (expressible only with a workaround) · `paper-cut` (works, reads wrong).

## Open items (derived — the entry below is always the source of truth)

**Four open — F-058 and F-059** (both opened 2026-08-06 by the `Toast`
anatomy review, fsl-ui ADR-040), **F-061** (opened 2026-08-06 by the E2
wave-1 parity gate) — those three `paper-cut` — **and F-063** (opened
2026-08-12 by the `InlineAlert` design, a `gap`): all four have the
measurement already taken and the repair deliberately not built, each with a
recommendation and a readmission condition in its own entry. Before them the
log had been at zero since F-004 resolved 2026-08-05. The grouping below (by the
_kind of decision_ each one needed rather than by severity, because that is
what made a review round plannable) is historical: every row in it is struck
through. Regenerate with
`grep -c '^- .*Status:\*\* open' FRICTION.md`, which counts entry lines only —
**not** by grepping the bare phrase, because that also matches the sentence
telling you to grep it, which made this count one too high twice.
Do not edit an entry through this list.

**Needs an owner decision on the colour/type model** — measured, options written, nothing to build until one is picked:

| #         | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Where the evidence is                                                                                                                                                                                                                            |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ~~F-053~~ | **Resolved 2026-08-05 (fsl-ui ADR-038): `StatusLight` adopts the reference's dot+label form.** A 10px dot (`radii.round`, `feedback.{evaluation}.background.default`) plus a label inked from the page's own default text, no fill; `Badge` keeps `CHIP_BOX` as its sole reader. Visible break on every existing consumer, accepted — no consumer depended on the pill's box for alignment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | measured in Chromium 2026-08-05; reference `status-light-dot-size-medium`                                                                                                                                                                        |
| ~~F-057~~ | **Resolved 2026-08-06, as a side effect of F-048 (fsl-ui ADR-037).** The collision was the border (`informational.{evaluation}.border.default`) sitting against a _different_ family's fill (`elevation.tonal[level]`); once the fill moved to the border's own role's `background.default`, the pair is exactly the one `colors.test.ts`'s `extractBorderBackgroundPairs` already audits for every `informational` role, in both modes. Measured in Chromium (`structure-surface--evaluations`), dark: `overlay`/`blocking` went from **1.00:1** (mirrored, no edge) to **2.16:1** (`muted`, `#6f6f6f` vs `#3d3d3d` border) and **1.67:1** (`primary`, `#161616` vs `#3d3d3d` border) — both `soft` (below the 3:1 an _occluding_ boundary owes, which `Surface` does not per ADR-031) but no longer mirrored. Light: 1.15:1 / 1.31:1, unchanged in kind. No repair to `elevation.tonal`'s ramp needed or made; the cross-cutting `semantic.elevation.edge.*` option this entry costed is no longer required by this defect. | measured in Chromium, both modes, 2026-08-06; fix in `packages/fsl-ui` (F-048/ADR-037), no `packages/fsl-theme` change                                                                                                                           |
| ~~F-051~~ | **Resolved 2026-08-06 (fsl-theme ADR-028, fsl-ui ADR-036): a cross-cutting `semantic.rail.track`.** Light `neutral.200` (7 units off the reference), dark `neutral.700` (4 units off, unchanged from F-050's already-correct value). All three components read the same address; `Slider`'s dark rail moves off `input.primary.background.disabled`, closing the "empty rail means disabled" defect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | measured in Chromium, both modes, 2026-08-05; reference `track-color`                                                                                                                                                                            |
| ~~F-052~~ | **Resolved 2026-08-06 (fsl-ui ADR-036): the ceiling ships as `--fsl-track-max-width`, unset by default.** No default cap (breaking for wide-container consumers); `Meter`'s reference `meter-default-width` not adopted — the reference itself gives `Meter` an intrinsic size and `ProgressBar` none, but this package already lets a host fix any block's width via `Box`/`Container`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |                                                                                                                                                                                                                                                  |
| ~~F-024~~ | **Resolved 2026-08-04 (ADR-030): the surface contract.** The painter publishes its fill on `--fsl-surface`; the quiet rung's resting fill/edge read `var(--fsl-surface, <own token>)`. Ruling kept whole — always paints, always opaque, theme pairs untouched — and the stratum-aware value lives where the knowledge lives: with the painter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `Overlay/ActionMenu` → `Emphasis`, dark: `#161616` on a `#262626` card. **Consumer found 2026-08-03**: Studio Team page, dark — the quiet `Remove` painted `rgb(22,22,22)` on a `rgb(61,61,61)` table row. Verified gone in Chromium 2026-08-04. |
| ~~F-043~~ | **Resolved 2026-08-04 (fsl-theme ADR-026).** The pairing suite now audits the **effective** ink for every declared background state (the `?? text.default` the components actually render), and a structural guard enforces "a mode only remaps" — 192 pairs entered, 7 values fixed across 3 classes, verified in Chromium.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Storybook `Overlay/Menu`, dark — measured 2026-08-03                                                                                                                                                                                             |
| ~~F-029~~ | **Resolved 2026-08-03 (ADR-028), refined 2026-08-04 (fsl-theme ADR-025)**: a part that paints no surface takes the stratum's ink, `consequence` selects it — and the ink is the cross-cutting `semantic.consequence.destructive.ink` (model §6, the focus-ring mechanism), so the cross-ux read retired and §1 is back to zero exceptions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `Overlay/ActionMenu`, any mode                                                                                                                                                                                                                   |
| ~~F-039~~ | **Resolved 2026-08-02.** The accent cascade was a real pairing-#2 violation and is fixed (brand.200). The focused-border half was **not** a defect: the docs already ruled it and had it backwards — the ring indicates on every entity, the border only tints, and no component uses the border alone. `colors.md` corrected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~F-027~~ | **Resolved 2026-07-31** (P3 Round 0, ADR-024). The border pairing now runs per mode, `disabled` is exempt and a mirrored edge is its own asserted set — which cut ~90 names to a readable list and turned 22 dark-only contexts into four omissions, all remapped. What it exposed and did not settle is F-039.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ~~F-021~~ | **Ruled 2026-07-29 (ADR-022): control type stays fluid** — the system's own identity; S2 is reference, not authority. Readmission: measured illegibility at 14px, or a coarse-pointer consumer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | measured 14–16px across the range                                                                                                                                                                                                                |
| ~~F-035~~ | **Fixed 2026-07-29 (ADR-022): the control inset is outcome-bearing and fixed** (`6px`/`12px`/`24px`) — measured 32/34/34/34 at 390–1920: the mid-range drift is gone; the 390px step is the fluid type meeting the `hit` floor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | measured at 390 / 900 / 1280 / 1920                                                                                                                                                                                                              |

**Component gaps — something to build, scope already understood:**

| #         | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~F-002~~ | **Resolved 2026-08-03.** `Link isCurrent` sets `aria-current="page"` and reads the `current` ink the theme always shipped. No vocabulary grew — `current` was already a legal State; the resolver was missing the mapping.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ~~F-004~~ | **Resolved 2026-08-05 (fsl-theme + fsl-ui, owner ruling delegated this round): a named `surface.card` step aliasing `core.sizing.ramp.layout.1`.** Measured which of the six `layout` ramp steps was unclaimed and closest to the finding's own 20–26rem (320–416px) target: only `layout.5` (`surface.maxWidth`'s own step) had a consumer, and `layout.1` (`clamp(320px, 40cqi, 480px)`) is the narrowest — floor lands exactly on the target's own floor, ceiling overshoots the target's 416px top by one step's headroom, the closest fit without a new formula. `Box.maxWidth`/`Container.size` both take `'card'` now. `LoginPage`'s own hand-rolled `min(24rem, calc(100% - 2rem))` — the exact workaround this entry described — is replaced with `Container size="card"`; the page's separate viewport-centering workaround (block-axis, not width) stays, since it is a different gap this entry never covered.                                                                                                                                                  |
| ~~F-010~~ | **Resolved 2026-08-02.** `Chip` — Structure entity, the descriptive member both references ship and we lacked. Box shared with `Badge` via `CHIP_BOX`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ~~F-016~~ | **Resolved 2026-08-02.** `List`/`ListItem` — a pair, as the reference ships it, because an `as` prop cannot enforce that a stack's children became list items. No vocabulary change was needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ~~F-023~~ | **Resolved 2026-08-02.** The root cause was the missing `Drawer`, not the grid. `Drawer` shipped + `AppShell sidebarVariant='temporary'`. Which variant applies stays the app's call, as in all three references.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ~~F-044~~ | **Resolved 2026-08-04** (fsl-theme ADR-027 / fsl-ui ADR-031). Neither candidate fix shipped: the root cause named a third shape — `semantic.overlay.outline`, cross-cutting per model §6 and a sibling of `scrim`. The instrument came first and retired the per-stratum design: one value per mode clears 3:1 against every stratum.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~F-045~~ | **Resolved 2026-08-04.** `inset.surface.xs → core.spacing.fixed.1`, fixed rather than fluid (ADR-022's argument one scale out). Menu surface 146px → 110px for the same three rows; chrome 34% → 13%.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~F-046~~ | **Resolved 2026-08-04.** `--fsl-dialog-min-width` ships beside the two knobs it pairs with, at the reference's 288px.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~F-047~~ | **Resolved 2026-08-04** at 200px — the reference's mobile value, because our label type is a step larger than theirs (F-021).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ~~F-050~~ | **Resolved 2026-08-05** (fsl-ui ADR-033). A Feedback fill and its rail resolved to the same colour in dark (1.00:1 on `primary`, both bundles) — the rail read `muted.border`, which lightens in dark onto `primary.background`'s own value. Fixed by reuse: the rail reads the quiet **surface**, which is what the docs already said. Guarded on both sides plus a theme-side value guard, verified to fail on injection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ~~F-054~~ | **Resolved 2026-08-06.** Both halves close the same way — the code was right, the table was incomplete. Forcing `ToastRegion` to the reference's 336px desktop cap in Chromium wraps a real description from one line to two (27px → 54px): the same type-runs-a-step-larger relationship F-021/F-047 already established, now verified for Toast's own copy, so `420px` stays at every viewport, documented next to the constant. And `Toast` frames a title + description the way `Dialog` frames prose — ADR-031's table now names both as the `md` exception instead of `Dialog` alone (measured in Chromium 2026-08-06, region forced to 336px).                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~F-055~~ | **Resolved 2026-08-06 (P3 round 4).** `GridListItem`/`ListBoxItem`/`TableRow` painted `input.primary.background.default` as their resting fill unconditionally. In light that value is byte-identical to the container's own background (both `neutral.0`) — invisible, and correct by luck. The dark alternate remaps it to a filled text-field box (`neutral.700`) while the container stays `neutral.900`: every row in `GridList`/`ListBox`/`Table` rendered a solid lighter block in dark mode, at rest, with no hover or selection. `Selection` has no `evaluation` to swap (the `MenuItem`/ADR-015 fix's mechanism) and no `input.*` role matches the container in both modes, so `resolveCollectionRowBackground` overrides only the row's `default` key with the container's own resolved background — hover/active/selected keep the entity's own values. Guarded from both sides in `collectionRow.test.tsx` (measured in Chromium, both modes: dark item fill `rgb(61,61,61)` on a `rgb(22,22,22)` container before, `rgb(22,22,22)` on `rgb(22,22,22)` after). |
| ~~F-056~~ | **Resolved 2026-08-06 (P3 round 6).** `Disclosure` and `Accordion` restated one design decision under two names — `DisclosureTrigger`/`AccordionTrigger`'s style builder, `DisclosurePanel`/`AccordionPanel`'s inner wrapper, and both roots' container chrome were byte-for-byte copies. Extracted to `src/tokens/disclosureAnatomy.ts` (`buildDisclosureContainerStyle`/`buildDisclosureTriggerStyle`/`buildDisclosureIndicatorStyle`/`buildDisclosurePanelBodyStyle`), the Disclosure-family counterpart of `ActionTrigger/anatomy.tsx`/`Field/anatomy.tsx`. `data-scope` stays per-file. Guarded directly in `disclosureAnatomy.test.tsx`; verified pixel-identical in Chromium, both modes.                                                                                                                                                                                                                                                                                                                                                                            |
| F-052     | A rail has no width envelope: measured 310 → 1840px against the reference's 48px floor / 768px cap. The **floor shipped**; the ceiling is authorial (it needs a default width to pair with) and is the open half.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ~~F-048~~ | **Resolved 2026-08-06 (fsl-ui ADR-037), owner ruling delegated this round.** `Surface` now reads `informational.{evaluation}.background.default` for fill at every level — the rule the six occluding overlays already use — instead of `elevation.tonal[level]`. Dark `Surface level="overlay"` and a `Dialog` panel now paint the identical `#161616` at `evaluation="primary"` (`#3d3d3d` vs `#161616` before). `elevation.tonal` stays defined, unread. Closes F-057 as a side effect (see that entry).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

**Contract / a11y debt — the component works but its published promise does not hold:**

| #         | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~F-026~~ | **Resolved 2026-07-29** (forms item D). All three components take `data-part="frame"` on the wrapper with `control` on the operated element; `KNOWN_NESTED_PAIRS` in invariant #12 is empty.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ~~F-049~~ | **Closed 2026-08-05 — will not do** (P3 round 2, ADR-032). An anchored surface announces as a `role="dialog"` wrapped around its menu: upstream's default for any popover not marked `isNonModal`, matched by the reference, and the branch that adds the role also adds the hidden dismiss buttons. What was verified instead is that the trigger's `aria-controls` resolves to the menu itself — now asserted.                                                                                                                                                                                                                        |
| ~~F-028~~ | **Resolved 2026-08-05, owner-delegated ruling.** The claim was correct and the July removal answered the wrong question: `useToolbar`'s generic, DOM-walking focus manager already jumps focus to the toolbar's boundary on `Tab`/`Shift+Tab`, no child cooperation required — the 2026-07-25 test saw the opposite only because `user-event`'s `tab()` can't observe a focus change made inside the same keydown. Verified by source, a `fireEvent.keyDown` jsdom test, and Chromium via Playwright against a mixed toolbar (fsl-ui trigger + plain host `<button>` + fsl-ui `Select`). No `Toolbar.tsx` change; docs/tests corrected. |

| ~~F-033~~ | **Resolved 2026-07-29** (Checkbox in forms A2; Switch in forms E). The Switch half closed by moving onto RAC's `SwitchField` root — the upstream had deprecated plain `Switch` and built the envelope's contexts into its replacement. |
| ~~F-032~~ | **Resolved 2026-07-29** (forms item F). The message read the control's readable-value ink; it now reads `input.negative.text.*`, the role Lexicon §10.15 puts on the reporting part. Two corrections were spent over-reaching to the control first. |
| ~~F-031~~ | **Withdrawn 2026-07-29** — not a defect. Hover is ruled not to apply while invalid; focus measured intact on the ring while the border keeps the valence. Nothing was hiding behind the cascade's shape. |
| ~~F-034~~ | **Closed 2026-08-02 — will not do.** Pairing #3(b) is conditional and the ring already indicates, so a focused fill would be reinforcement, not replacement. The real half (a ring clipping inside a 32px row) was fixed in forms C4. Readmission criterion recorded. |
| ~~F-036~~ | **Resolved 2026-07-31** (P3 Round 0, ADR-024). A cross-role inventory pairs a part's ink against the surface it renders on; the correction the measurement forced is that the surface is the four `informational` strata, not one page token — this entry's own 7.97 was the raised card, not the page. |
| ~~F-037~~ | **Resolved 2026-07-29** (forms item I). `FormSubmit`'s JSDoc promised `data-pending` and `data-composition`; the DOM carried neither — React Aria clobbers both after the passthrough spread. First consumer found it two items after the promise was written. |
| ~~F-038~~ | **Resolved 2026-07-30** (ADR-023, owner review of #1181). ADR-022 wrote the fixed control inset as a literal `6px` in the **semantic** layer under a `RawValue` necessity that was circular — "core has no fixed step" is a missing core token, not an impossibility. Moved to `core.spacing.fixed.*`; the semantic guard's exception list is gone. |

**Naming / discoverability:**

| #         | What                                                                                                                                                                                                                                           |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~F-040~~ | **Resolved 2026-08-02** (owner decision). Renamed to Spectrum's split: `Badge` is descriptive, `StatusLight` carries the valence, `TagGroup` stays the interactive set. Scopes moved with the names.                                           |
| ~~F-041~~ | **Closed 2026-08-02 — the docs had already placed it.** `breakpoints.md` puts local aliases (its own example: `navCollapse`) in the application layer, and forbids components behaving on viewport thresholds. Neither package ships the hook. |

| ~~F-042~~ | **Resolved 2026-08-03**, one day later, by the fix it predicted. Links have no collection owner, so the drawer holds them; Meridian runs the temporary shell below `md`. |

**Ecosystem / token vocabulary:**

| #         | What                                                                                                                                                                                                                                                                                   |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~F-005~~ | **Resolved 2026-08-02** (ADR-027 supersedes ADR-004). The recipe names `react-hook-form`/`zod`/`@hookform/resolvers` directly and `@ttoss/forms` leaves the package. Its own workaround line had gone stale: the Studio imports none of them — it runs React Aria's native validation. |
| ~~F-020~~ | **Resolved 2026-08-02.** `core.border.offset.focused` + `semantic.focus.ring.offset`; kept independent of the ring's width, as the reference keeps them.                                                                                                                               |

---

## Entries

### F-001 — `llms.txt` catalog missing the composition layer

- **Date:** 2026-07-22 · **Surface:** `packages/fsl-ui/llms.txt` · **Severity:** gap · **Status:** ✅ fixed (same day)
- The machine-readable catalog omitted `AppShell`, `Box`, `Grid`, `Container` (Structure), `Badge` (Feedback), and `Code` (Structure) — the ADR-009 presentational layer never landed in the AI surface. An agent consuming only `llms.txt` (the shipped promise) cannot discover the layout primitives and would hand-roll layout CSS.
- **Action:** entries added to the `llms.txt` catalog.

### F-002 — `Link` has no `current`-state affordance

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` `Link` / `resolveInteractiveStyle` · **Severity:** gap · **Status:** ✅ resolved 2026-08-03 (`Link isCurrent`)
- The theme ships `colors.navigation.*.text.current`, but `InteractiveStates` has no `current` entry and `Link` never reads it — `aria-current` renders identically to any other link. A sidebar built from `Link`s cannot mark the active page without host CSS.
- **Workaround:** the Studio shell uses a vertical `Tabs` as primary navigation (Navigation entity; selected state and keyboard support from React Aria).
- **Backlog:** support `current` on `Link` (RAC exposes no `isCurrent`, so likely via an explicit prop mapped to `aria-current` + the `current` color).
- **Resolved 2026-08-03 exactly as the backlog specified, and nothing in the vocabulary grew.** `current` was already a legal State in the registry and already listed in `CONTEXT_EXTRA_STATES.navigation`; what was missing was the resolver's flag→state mapping and a component to pass it. `STATE_PRIORITY` gains `isCurrent → current` and `Link` gains `isCurrent`, which sets `aria-current="page"` and resolves `navigation.{role}.text.current`. **This was implementation catching up with the contract, not a token decision** — the owner's standing rule (no token leaves while the package stabilises) is satisfied in the strongest way available: the token now has a reader.
- **Cascade placement, and why:** `current` outranks `checked` because it is the more specific claim about the same kind of fact — `colors.md` § Picking a state says a tab representing the live route is both selected _and_ current — and both sit under `disabled`, because unavailability is the more urgent thing to announce. Pinned by the cascade-order test and asserted through the component.
- **`aria-current="page"`, not `true`:** the link names a destination, so the specific token is what AT should read, and it is what APG's navigation pattern asks for.
- _Studio:_ the sidebar became a real `nav` landmark of `Link`s — which closed F-017 and F-042 with it.

### F-003 — `Tab` selected indicator is block-end even in vertical orientation

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` `Tab` · **Severity:** paper-cut · **Status:** ✅ fixed (P3 polish pass)
- The selected-tab indicator is positioned `insetBlockEnd: 0` unconditionally, so in `orientation="vertical"` (sidebar use) it underlines each item instead of marking the start edge — the conventional affordance for vertical navigation.
- **Action:** `Tabs` now provides its orientation through a context; `Tab` renders the indicator on the inline-start edge when vertical (block-end underline stays for horizontal). Regression tests cover both edges.

### F-004 — no named width threshold for a narrow centered card

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` `Box`/`Container` sizing vocabulary · **Severity:** gap · **Status:** ✅ resolved 2026-08-05 (fsl-theme, fsl-ui), owner ruling delegated this round.
- `Box.maxWidth` and `Container.size` offered only `surface` (page cap) and `reading` (long-form measure). An auth card (~20–26 rem, centered) — one of the most common real layouts — had no in-system cap.
- **Workaround (now retired):** `LoginPage` hand-rolled `min(24rem, calc(100% - 2rem))` on a bare `div` for the width half. (The Blocks-gallery `Grid minColumnWidth="lg"` workaround this entry originally named was never what shipped — the actual login page had its own bespoke rule, closer evidence than the gallery's side-by-side layout, and is what the fix retires.)
- **Ruling: option (a), a named `card` step — reusing an existing ramp rung rather than inventing one.** `core.sizing.ramp.layout` already carries six container-fluid steps for exactly this kind of bound (`sizing.md`'s own stated pattern: semantic fluid tokens alias ramp steps, never a new formula). Checked every step for a collision first (model.md §6, reuse-before-creation cuts both ways): only `layout.5` had a consumer (`surface.maxWidth`), so `layout.1` — `clamp(320px, 40cqi, 480px)` — was free. Its floor (320px) is exactly the finding's own 320px floor; its ceiling (480px) overshoots the finding's 416px top by one ramp step of headroom, the closest available fit with no new formula and no ramp growth. `surface.card` ships in `fsl-theme`'s `SemanticSizing`, aliased in `baseTheme.ts` (inherited by `bruttal`, which never overrode `sizing`); `Box.maxWidth`/`Container.size` both gained `'card'`, reading `vars.sizing.surface.card`. Guarded in `fsl-theme`'s `sizing.test.ts` (resolves inside the ramp, distinct from both neighbours — verified to fail on injection) and in `fsl-ui`'s `Box.test.tsx`/`Container.test.tsx`.
- **Consumer:** `LoginPage`'s width workaround is retired in favour of `Container size="card"` — verified in Chromium at 390/900/1280px, light and dark: a narrow centered card, not a full-width one. Its separate viewport block-centering workaround (unrelated width axis, never covered by this entry) stays, since no primitive fills the viewport block size and that gap is not filed.
- Option (b), `Grid`-level column caps, was not the fix: it is the workaround this entry itself named as insufficient for a standalone card page, so implementing it would not have closed the gap.

### F-005 — ADR-004 forms recipe drags the legacy field suite

- **Date:** 2026-07-22 · **Surface:** fsl-ui ADR-004 / `@ttoss/forms` packaging · **Severity:** gap · **Status:** ✅ resolved 2026-08-02 (ADR-027)
- The sanctioned bridge recipe imports `Controller`/`useForm`/`z`/`zodResolver` from `@ttoss/forms`, whose only entry also exports the legacy `FormField*` suite with `@ttoss/ui`, `@ttoss/components`, and `@ttoss/react-i18n` as peer dependencies — unacceptable baggage for an fsl-first app.
- **Workaround:** the Studio imports `react-hook-form`, `zod`, and `@hookform/resolvers/zod` directly (same versions syncpack enforces).
- **Backlog:** a lean re-export subpath (e.g. `@ttoss/forms/core`) or update ADR-004's recipe to name the direct imports.
- **Resolved 2026-08-02 the second way (ADR-027 supersedes ADR-004).** ADR-004 says the recipe _is_ the integration test, so rewriting the recipe meant rewriting the test's imports: `react-hook-form`, `zod` and `@hookform/resolvers/zod` by name, and `@ttoss/forms` leaves this package including as a devDependency. The subpath was rejected because it changes a package that is being discontinued in order to serve a re-export nobody needs once the recipe names its upstreams.
- **Its own workaround line had gone stale, which is the more useful finding.** This entry recorded "the Studio imports react-hook-form, zod and the resolver directly" — measured today, **the Studio imports none of them**. It moved to React Aria's native validation during the forms work, so the default path uses no form library at all. That reframes the bridge: it is not how fsl-ui does validation, it is the recipe for an app that _already_ runs react-hook-form, and such an app has those packages directly.

### F-006 — `TabList` ignored vertical orientation

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` `TabList` · **Severity:** blocker (for sidebar navigation) · **Status:** ✅ fixed (same day)
- The JSDoc promised "a row (or column when the parent `Tabs` is vertical)", but the style was a static row with a block-end divider — `orientation="vertical"` rendered the sidebar tabs horizontally, overflowing the rail. Found by the Studio shell's visual check, invisible to the DOM-level test suite.
- **Action:** `TabList` style now reads the RAC `orientation` render prop — column direction, `gap.stack`, and an inline-end divider when vertical. Regression tests added for both orientations.

### F-007 — multi-field rows have no column alignment: Table demanded

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` catalog (Settings block) · **Severity:** gap · **Status:** ✅ fixed (same day — `Table` shipped, Settings retrofitted)
- The team-members list is name + email + role + actions per row. `GridList` rows are independent flex lines, so fields do not column-align across rows (badges and actions sit ragged, scanning down a column is impossible), and there is no columnheader semantics or sorting. This is the evidence the ROADMAP predicted: a Settings/CRUD flow demands `Table` (root + columnheader + row + cell), not a list.
- **Action:** `Table`/`TableHeader`/`TableColumn`/`TableBody`/`TableRow`/`TableCell` shipped in fsl-ui (ADR-007 split, sorting via `action.sortAscending`/`sortDescending` intents, row-click selection). The Settings block now renders a sortable table. Deferred until demanded: checkbox selection column, column resizing, virtualization.

### F-008 — `Select` without typeahead: ComboBox demanded

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` catalog (Settings block) · **Severity:** gap · **Status:** ✅ fixed (2026-07-24 — `ComboBox` shipped, invite flow retrofitted)
- The invite flow was designed with a timezone field and the field was **dropped** because a 30+-option `Select` popover is scan-only — no typeahead, no filtering. A component gap changed the product design; that is the strongest form of adoption evidence. Role (3 options) stayed a `Select`, which is its correct scale.
- **Action:** `ComboBox`/`ComboBoxItem` shipped in fsl-ui (Wave 3) — text input with a typeahead-filtered list, a `validationMessage` part `Select` still lacks (F-009), and the `--fsl-combo-box-max-height` knob so a long list scrolls instead of running off-screen. The Meridian invite dialog now carries the restored **Timezone** field over 35 zones, and the roster shows the column. **ADR-012** records the governing call the component forced: a freeform channel makes a picker `Input`, not `Selection` — so `ComboBox` is Input while `Select` stays Selection, and the deferred `Autocomplete` inherits the rule.
- **FSL validation note:** the ROADMAP's anatomy claimed "taxonomy additions needed: none", which held only half. `item` was already legal on Selection, but `trigger` is not a legal `Input` role — the chevron ships as an internal data-part with no `*Meta`, the same resolution NumberField's steppers and Slider's track reached (ADR-008). Zero taxonomy changes, third component in a row.

### F-009 — `Select` has no validation-message part

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` `Select` (+ `Text` tones) · **Severity:** gap · **Status:** ✅ fixed (2026-07-28 — forms C2)
- `TextField` composes `TextFieldError`; `Select` only tinted its trigger via the `invalid` State — there was nowhere to render the message. The invite form hand-assembled a live-region `Text` under the Select, and `Text` has no negative/danger tone, so the message could not even be tinted in-system.
- **Action:** `Select` takes `description` + `errorMessage`, rendering React Aria's `Text slot="description"` and `FieldError` — which work here although they are dead on a lone `Checkbox`, because `Select` supplies a `FieldErrorContext` holding its real validation state (read in `Select.mjs`). Both are INTERNAL data-parts, the resolution `CheckboxGroup` already reached: the Selection entity has no `description`/`validationMessage` structural role and the evidence still does not justify widening the vocabulary.
- **The gap was a class, not an instance, and measuring it is what showed that.** Probed across all nine field roots before writing anything: the necessity marker B1 shipped reached **three** of them, `RadioGroup` had F-009's exact shape one family over, and three files carried a private helper computing colours the anatomy already computed. So the fix is one shared envelope (`FieldLabelPart` / `FieldDescriptionPart` / `FieldValidationMessagePart` in `Field/anatomy.tsx`), taking `scope` as a prop so no published attribute changes, with a table-driven guard over the family and a named exception list for `Switch` and `Slider`.
- **Found while measuring it, and fixed in the same pass:** a split control's **frame** declared no type, so the same `ComboBox` resolved `16px` in Storybook and `18px` inside the Studio's invite dialog — an undeclared frame inherits the host's paragraph size and hands it to every adornment in it. The row's type now sits on the frame too, asserted by invariant #11 for both control shapes.
- **The second half closed on 2026-07-29** (F-032, forms item F). It had been re-measured on `Select`'s new part in both modes — `rgb(22,22,22)` light, `rgb(255,255,255)` dark, trigger border `rgb(220,38,38)` / `rgb(252,165,165)` byte-identical to `TextField`'s — and the message now reads `input.negative.text.*`, resolving `rgb(185,28,28)` / `rgb(252,165,165)`. So F-009 is closed end to end: the message part exists on every field, and it carries valence.

### F-010 — no neutral tag primitive for descriptive labels

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` `Badge` semantics · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-02 (`Chip`)
- Role chips ("Admin", "Editor") are descriptive, not evaluative — but the only chip-shaped primitive is `Badge` (Entity = Feedback, valence vocabulary). The block uses `Badge` with the neutral `primary` evaluation, which renders fine but blurs the Feedback entity. `TagGroup` is Selection (interactive), so it is not the answer either.
- **Backlog:** decide whether descriptive chips are a legitimate `Badge` use (document it) or a small Structure-entity `Tag` primitive.
- **Resolved 2026-08-02 the second way, and the references decided it rather than taste.** Both split this axis three ways and we had shipped two of the three: Spectrum has Badge (metadata) / StatusLight (semantic status) / TagGroup (user-managed); Chakra has Badge (plain span) / Status (root+indicator) / Tag (compound with a close trigger). Ours are `Badge` — Feedback, i.e. the member Spectrum calls **StatusLight** — and `Tag`, the Selection item inside a `TagGroup`. The static descriptive chip was the missing middle, and it is a different **entity** from both rather than a variant: it neither reports (Feedback is "the user is the audience of a system-initiated outcome") nor is operated (Selection). That is Structure → `informational`.
- **Named `Chip`, not `Tag`, because `Tag` was already taken** by the interactive member — and that collision surfaced the larger finding now filed as F-040: both references call the _descriptive_ chip `Badge`, so our two published names are inverted relative to them.
- **Guarded as a class, not as a component.** The box moved into `CHIP_BOX` (`src/tokens/chipBox.ts`) with `Badge` refactored onto it, because two chips side by side must not be able to disagree about their own roundness — the drift ADR-013 and F-022 each paid for. The test asserts the _rendered_ box of both, so a component that stops reading the shared source fails even while the module still exists.
- _Studio:_ three sites converted — the team roster's role, an environment's type, and the workspace name beside the product mark. All three were labels; none was an outcome.

### F-011 — Table sorting types leaked a react-aria-components import

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` `Table` API · **Severity:** paper-cut · **Status:** ✅ fixed (same day)
- Typing `sortDescriptor`/`onSortChange` state required importing `SortDescriptor` from `react-aria-components` — a transitive dependency the consumer would have to install directly (pnpm isolation blocks the import otherwise). Found while retrofitting this block.
- **Action:** fsl-ui re-exports `SortDescriptor`/`SortDirection` from its root entry.

### F-012 — icons never rendered in production builds (silent addIcon rejection)

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` Icon layer (`glyphs.ts`) · **Severity:** blocker · **Status:** ✅ fixed (same day)
- Two stacked silent failures, found only by checking a real browser (the Table sort arrow was missing — then so was every other glyph: Select chevron, Toast close, Checkbox check): (1) camelCase intents produced Iconify-invalid registry names (`fsl-ui:action-sortAscending` — Iconify allows only lowercase `[a-z0-9-]`); (2) far worse, Node-mode CJS interop in Vite/Rolldown bundles wraps the per-icon lucide modules so `addIcon` received `{ __esModule, default: data }` instead of the icon data — **every registration since B1 was silently rejected in production builds** and every icon fell back to a (blocked) Iconify API fetch. Jest never saw it: Babel interop unwraps `.default`, and jsdom asserts attributes, not rendered glyphs.
- **Action:** `iconifyName` kebab-cases camelCase humps; `unwrapGlyph` normalizes both interop shapes; `ensureIconGlyphs` now **throws** when `addIcon` returns false (registration failure is a hard bug, never silent); regression tests for name validity. Verified in the built Studio: glyph status `rendered`, sort arrow visible.
- **Lesson for the gate:** DOM-level suites cannot see this class of failure — the per-block visual check in a real browser is load-bearing, not cosmetic.

### F-013 — dataviz extension ships no typed vars mirror

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-theme/dataviz` · **Severity:** gap · **Status:** ✅ fixed (2026-07-24 — `datavizVars` shipped)
- The foundation ships `vars` (typed CSS-var mirror), but the first-party dataviz extension does not — chart code must hand-roll the README's `buildVarsMap` recipe, including an `as CssVarsMap<Extended>` cast (the documented direct assignment does not type-check against the widened shape). Every dataviz consumer will repeat this boilerplate.
- **Workaround:** `src/theme.ts` builds `studioVars` with the cast.
- **Action:** `datavizVars` shipped from `@ttoss/fsl-theme/dataviz` — a typed `var(--tt-dataviz-*)` mirror of the dataviz semantic subtree (names derive from paths, so it is theme-independent). The Studio chart consumes it; the cast recipe stays documented for third-party extensions.

### F-014 — no display-scale Text variant for stat values

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` `Text` vocabulary · **Severity:** gap · **Status:** ✅ fixed (P3 polish pass)
- KPI tiles want a large numeral, but `Text` caps at `body-lg` and the display/headline type steps are reachable only through `Heading` (h1–h6 document semantics — a stat value is not a heading). The dashboard's numbers render at body scale, visibly under-weighted for the pattern.
- **Action:** `Text` gained `variant="display-sm"` — the display type step without heading semantics, documented as the stat step. The dashboard KPIs and pricing amounts now use it with `numeric="tabular"`.

### F-015 — feature-list glyphs demanded a public Icon

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` Icon (ADR-005 internal-only clause) · **Severity:** gap · **Status:** ✅ fixed (same day — ADR-010)
- The Pricing block's feature lists need checkmark glyphs outside any shipped component — the exact promotion trigger ADR-005 left open. Without a public `Icon`, blocks would hand-author SVG (the drift ADR-005 exists to prevent).
- **Action:** `Icon` promoted to a public export (ADR-010) with `iconMeta`, intent types, and the curated `ICON_INTENTS` registry; `status.success` intent added (circled check, distinct from `selection.checked`). The standalone `@ttoss/fsl-icon` package stays parked — its named trigger is a consumer that wants icons without fsl-ui.

### F-016 — no semantic list primitive for content lists

- **Date:** 2026-07-22 · **Surface:** `@ttoss/fsl-ui` catalog (Pricing block) · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-02 (`List`/`ListItem`)
- Marketing feature lists want `ul`/`li` semantics, but `Stack` renders a div and `Text` renders only p/span/div. The block hand-applies `role="list"`/`role="listitem"` on Stacks — accessible, but every content list will repeat this.
- **Backlog:** a `List`/`ListItem` Structure primitive, or an `as`/role affordance on `Stack`, via governance.
- **Resolved 2026-08-02 the first way, and the reference settled it.** Chakra ships `List.Root` + `List.Item`, not a polymorphic stack, and the reason generalises: a list is not one element but a **pair whose parts are only valid together** — an `<li>` outside a list owner is loose content — and an `as` prop on `Stack` cannot enforce that its children became list items. Making it a pair is what lets the identity be stated per part.
- **No governance was needed after all.** The entry assumed a vocabulary change; the parts are `Structure`/`root` and `Structure`/`content`, both already legal, and the component paints nothing — a frame-only Structure lawfully reads a subset of its row.
- **`variant='plain'` is the default** because it is the shape most product lists want and the one that otherwise gets hand-rolled: `<ul>` semantics kept, marker hidden, and the indent the browser reserves for the absent marker reclaimed rather than left as the hanging space every reset strips by hand.
- _Studio:_ the Billing plan's feature list drops its `role="list"`/`role="listitem"` workaround.

### F-017 — Tabs-as-navigation demands panel co-location and a width workaround

- **Date:** 2026-07-24 · **Surface:** `@ttoss/fsl-ui` `Tabs` (S2 shell) · **Severity:** gap · **Status:** ✅ resolved 2026-08-03 — the workaround it documented is gone
- Two stacked findings while wiring the S2 sidebar navigation with the recorded F-002 workaround (vertical `Tabs` as primary nav): (1) a `TabList` with **no TabPanels** emits `aria-controls` pointing at a nonexistent panel — axe fails with `aria-valid-attr-value`, so the nav-only usage v2 shipped was silently invalid ARIA; (2) fixing it by spanning one `Tabs` scope across the app frame (TabList in the AppShell sidebar, TabPanel in the main region — RAC context supports the separation) collides with the `Tabs` root's co-located layout: it imposes `display: flex; flex-direction: row`, which shrinks an `AppShell` child to content width.
- **Workaround:** the whole frame lives inside one `Tabs`; a `Box width="full"` wrapper restores the AppShell width; each route's page renders inside its real `TabPanel`, so tab semantics are genuine (selection = client-side routing) and axe passes.
- **Backlog:** strengthens F-002 — primary navigation wants a real affordance (Link `current` state or a Navigation-entity nav list), not tab semantics contorted around an app frame.
- **Resolved 2026-08-03 by removing the workaround rather than by fixing `Tabs`.** Both findings here were consequences of using tabs for navigation: no panel-less `TabList` (so the whole frame had to live inside one `Tabs` scope) and no width workaround (so `Box width="full"` had to undo the tab row's layout). With `Link isCurrent` shipped the sidebar is a `nav` landmark of links, the `Tabs` scope is gone from `AppFrame`, and both workarounds went with it — including the `Box width="full"` this entry filed.
- **Nothing about `Tabs` changed, and nothing should have.** The component was never wrong; it was being asked to be navigation. The entry's real content was the cost of that ask, and the cost is now paid down.

### F-018 — the container-fluid engine has no container: `cqi` resolves against the viewport everywhere

- **Date:** 2026-07-24 · **Surface:** `@ttoss/fsl-theme` fluid scales + `@ttoss/fsl-ui` layout primitives · **Severity:** gap · **Status:** ✅ fixed (same day — fsl-ui ADR-011)
- The theme's type/spacing ramps are declared **container-fluid** (`families/typography.ts`: "container query units (cqi) as the preferred fluid step"; ADR-019/020: "layout adapts to _container_ (cqi)") — but no `container-type` is ever established: not by `ThemeProvider`/preflight, not by `Surface`/`Grid`/`Container`/`AppShell`, and no doc instructs the host to create one. Per the CSS spec, `cqi` without an ancestor container resolves against the viewport, so the engine is viewport-fluid in practice.
- **Observed failure (Dashboard KPI tiles):** `display.3 = clamp(28px, calc(1.6cqi + 20px), 40px)` rendered 36.384px inside a 220px grid track at a 1024px viewport (1.6% × 1024 + 20) — type and inset sized for the page, not the tile; the value+delta row (196px min-content) overflowed the card by 11px. Had the tile been the container, the clamp would have bottomed out at 28px and the content would fit. The whole class "oversized internals in narrow containers" (grid tiles, sidebars, asides) follows.
- **Workaround (Studio):** the delta badge moved to the label row (`justify="between"`, `wrap`) and the KPI grid floor rose to `minColumnWidth="sm"` — floors must currently be picked against content min-width at the **viewport-resolved** clamp values, which is guesswork.
- **Action:** fsl-ui **ADR-011** — definite-width layout primitives establish `container-type: inline-size`: `Grid` hosts each child in a `data-part="item"` size container, `AppShell` marks its four regions, `Container` marks its root; `Surface` is explicitly rejected (content-sized in horizontal Stacks — containment would collapse it). Verified in a real browser: tile internals now scale to the track (the KPI grid returned to the `xs` floor, four-across, no overflow at 800–1920px). The containment contract is stated in fsl-ui `llms.txt`; the full resolved-type guarantee (browser-measured) is noted in ADR-011 as jsdom cannot compute container queries.

### F-019 — anchored popovers size to content, not to their trigger

- **Date:** 2026-07-24 · **Surface:** `@ttoss/fsl-ui` `ComboBox` / `Select` popovers vs the CONTRACT var-namespace ban · **Severity:** paper-cut · **Status:** ✅ fixed (2026-07-28 — forms C3, ADR-023)
- Found in the mandatory browser check of the S2 invite dialog: the timezone `ComboBox` popover renders **158px wide against a 307px input** (126px in dark, where the filtered set is one short label). Every reference-grade combobox matches the list width to the field — a list visibly narrower than the control it belongs to reads as unfinished. `Select` has the same behaviour, so this is a shared trait of the anchored-surface pattern, not a ComboBox regression.
- **Cause:** React Aria publishes the measured trigger width to the popover as `--trigger-width`, but CONTRACT §7 / the contract test ban reading any CSS variable outside the `--tt-`/`--fsl-` namespaces, so no component may consume it. The ban is right in the general case (it stops arbitrary vars leaking into consumers) and simply has no carve-out for vars the underlying primitive itself publishes.
- **Not worked around:** consuming `var(--trigger-width)` would need a contract change, and inventing an `--fsl-combo-box-min-width` knob would only paper over it with a number nobody can pick correctly. Filed instead of patched.
- **Action (ADR-023):** the namespace rule admits a **named allowlist** of custom properties published as documented API by a direct dependency, read through `upstreamVar(name, fallback)`. `--trigger-width` is the only entry, and `--trigger-anchor-point` was **not** admitted — nothing needs it, and an allowlist that grows by anticipation is not an allowlist. A picker's popover now declares `min-width` (S2's unconditional floor) plus a knob-overridable `width` (its default), so a host can widen the list and never narrow it below the row.
- **Both authorities settled the behaviour, and they draw the same line:** React Aria's own `Select`/`ComboBox` examples style their popovers `width: var(--trigger-width)` and its `Menu` example sets none; S2 documents `menuWidth` on `Picker`/`ComboBox` as "By default, matches width of the trigger. Note that the minimum width of the dropdown is always equal to the trigger's width" and has no such prop on `Menu`. **A menu is not a picker** — a picker shows the field's value space, a menu shows things to do — so our `Menu` keeps its own `--fsl-menu-min-width` (measured 192px against a 108.88px trigger: correct, unchanged) and `Popover` keeps its max-width.
- **Measured after, all four combinations:** popover width now equals trigger width exactly — Select 1200/1200 and 310/310, ComboBox the same; in the Studio's invite dialog both the Role dropdown and the Timezone list report **426px against a 426px field**. Stress case at a 140px trigger: the option "Administrator with billing access" wraps to three lines with no overflow in either direction.
- **The enforcement lesson outlived the fix.** The pre-existing ban was a source-text regex over `src/components/**`; routing the read through a helper in `src/tokens/` slipped past it and the suite stayed green through exactly the change the rule existed to catch. The binding check is now over the **rendered** inline styles of every DOM fixture, plus a source check that nothing _writes_ an allowlisted name — React Aria stops observing the trigger the moment `--trigger-width` is supplied, so a write would freeze the popover at its first-paint width. All three guards were verified to fail on an injected violation before being trusted.
- **Deliberate no-change:** the open popover overlays the description below the field. React Aria anchors it to the trigger (`placement: 'bottom start'`), so it covers what is beneath — which is what an overlay is, and what both reference implementations do. This entry mentioned the overlap alongside the width; only the width was a defect.

### F-020 — focus-ring gap is not a theme token

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-theme` `semantic.focus.ring` / `@ttoss/fsl-ui` `tokens/focusRing.ts` · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-02 (`focus.ring.offset`)
- Found in the P3 Slice 2 review: the reference system treats the gap between a control's edge and its focus ring as a first-class token (Spectrum: 2px ring + 2px gap), but `semantic.focus.ring` carries only `width`/`style`/`color` — the offset had no home, so components applied it ad hoc (11 of the ring's call sites were flush while `Select`/`Checkbox`/`Switch` floated at 2px — an inconsistency invisible to DOM-level tests).
- **Workaround (shipped with Slice 2):** `FOCUS_RING_OFFSET = '2px'` as a named constant in fsl-ui `tokens/focusRing.ts` (the ring's single owner), applied to all flush call sites; clipped-container insets (menu items, table rows) stay bespoke by design.
- **Backlog:** promote the gap to `semantic.focus.ring.offset` via governance so themes can retune it (a compact theme may want 1px, a playful one 3px). Type-level change to `ThemeTokens` — beyond a P3 value tune.
- **Resolved 2026-08-02 exactly as the backlog specified.** `core.border.offset.focused` holds the value (core is the layer that holds values — ADR-023's rule that a bare constant is never a lawful semantic value applies here too) and `semantic.focus.ring.offset` references it; `FOCUS_RING_OFFSET` in fsl-ui now reads the token instead of the `2px` literal it shipped as.
- **Kept independent of `width.focused` although the base theme sets both to the same value**, because the reference does the same — `focus-indicator-gap` and the indicator thickness are two tokens that happen to agree — and a theme retunes them for different reasons: thickness for prominence, gap for how much the ring breathes off the control.
- **`FOCUS_RING_INSET` deliberately did _not_ move onto it.** The inset is arithmetic, not taste: a ring needs `offset + width` of room outside the box, so at `-width` it needs none. Tying it to the gap would break the property its JSDoc argues for. Both halves are now asserted.
- **One stale artefact fixed alongside:** `families/focus.ts` still carried the focus-token decision table that F-039 corrected in `colors.md`, and its example wrote `outline-offset: 2px` as a literal — the very thing this entry is about.

### F-021 — control text is container-fluid, so it shrinks exactly where it should grow

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-theme` `core.font.scale.text.*` consumed by `semantic.text.{label,action}` · **Severity:** gap · **Status:** ✅ closed as working-as-designed (2026-07-29, owner ruling — ADR-022)
- Found while measuring Button against the reference system (P3 Slice 4 ①). Control text rides the container-fluid ramp (`text.2 = clamp(14px, 0.7cqi + 11px, 16px)`), so a Button in a narrow container renders **14px** and the same Button on a wide surface renders **16px**. The reference system moves the opposite way: its control text is a fixed step per size that steps **up** on touch/mobile (14px → 17px), because a narrow viewport usually means a finger, not a smaller need.
- ADR-019/020 already ruled that _control geometry_ is not container-fluid (`hit` is rem-anchored, with a coarse-pointer override) — but the ruling was never extended to control **type**, which still rides `cqi`. The result is a control whose target grows for touch while its label shrinks.
- **Not worked around:** nothing in the Studio is broken by it today (the measured range is 14–16px, both legible), and picking a direction is a theme-model decision, not a value tune.
- **Backlog:** an ADR extending ADR-019's non-fluid ruling from control geometry to control typography — either a rem-anchored control type step, or a coarse-pointer step-up mirroring `applyCoarseHitOverrides`. Touches `families/typography.ts` and the emitter, so it is governance, not P3.
- **Ruled 2026-07-29 (owner; recorded in fsl-theme ADR-022): control type stays fluid.** The owner's framing decided it — S2 is a reference, not an authority, and fluid-from-one-engine is this system's own identity (`typography.md`, `spacing.md`). The geometry half of the same question went the other way in the same ADR (the control _inset_ became a fixed contract, closing F-035), and the split is principled: the inset is outcome-bearing (it determines the control's box against ADR-019/020's own ruling), while the type is rhythm and both ends of its range (14–16px) are legible. **Readmission criterion:** measured evidence of illegibility at the 14px end, or a coarse-pointer consumer demanding S2's step-up — which would be a new pointer hook on the typography family, not a value tune.

### F-022 — glyphs sat 2px high wherever their host was not a flex container

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-ui` `Button` / `Select` / `Disclosure` / `Accordion` / `Checkbox` glyph hosts · **Severity:** paper-cut · **Status:** ✅ fixed (same day)
- Reported by the owner on the new Button icon ("o ícone está deslocado pra cima"), then measured across every glyph-bearing component in a real browser. The offset correlated exactly with the host's `display`: hosts that happened to be flex containers measured **0**, hosts in normal flow measured **−2px** (`Button` icon, `Select` icon, `Disclosure`/`Accordion` indicator) or **−1px** (`Checkbox` indicator).
- **Cause:** `iconify-icon` is an inline-level box, so in a non-flex host it participates in **baseline** alignment — the glyph's bottom sits on the host's text baseline and the descender space of the host's inherited font falls below it. The host box ended up 4px taller than the 18.19px glyph (22.19px), and centring the _host_ therefore left the glyph high by half the difference. `Icon` already set `lineHeight: 1` on itself, which does nothing: the strut belongs to the host, not the glyph.
- **Action:** `src/tokens/iconSlot.ts` — `ICON_SLOT_STYLE` (inline-flex + centred + `flexShrink: 0`), applied to every glyph host (`Button`, `Select`, `Disclosure`, `Accordion`, `Checkbox`, and `SearchField`'s adornment for consistency). Re-measured in Chromium: **offset 0 in all 16 cases**, and each host's height now equals its glyph's. Deliberately _not_ fixed by blockifying `Icon` itself — the public `Icon` must stay usable inline within a text run (the F-015 feature-list case), so the host owns the layout and the glyph stays layout-agnostic.
- **Guard:** contract invariant #9 (`components.contract.test.tsx`) asserts every span-shaped glyph host declares the centring flex box, across all DOM fixtures — a new component cannot reintroduce the drift.
- **Lesson (again, after F-012):** this class of defect is invisible to DOM-level suites _and_ to a casual look at a screenshot — it took a measured comparison of computed boxes. The per-delivery browser check needs to include measurement, not just eyeballing.
- **Second round, same day — box-centred is not optically centred.** With the hosts fixed, the owner reported the glyph now reading _low_. Box measurement said 0 (glyph centre = label-box centre = button centre), so the answer had to come from measuring **ink**: the drawn glyph spanned 13.64px against the label's 12px cap height, overhanging the cap line by 0.82px and — the part the eye actually catches — the **baseline** by 0.82px. Anything crossing below the baseline reads as sinking. Both owner reports were correct; the second was a _size_ defect, not a _box_ defect.
- **Action (round 2):** new sizing step `semantic.sizing.icon.text` → `{core.sizing.relative.em}` (1em), exposed as `<Icon size="text">`, forced by `Button` on its glyph and adopted by the `Select`/`ComboBox` chevrons (same optical situation — a glyph beside its value text). Because the step is _relative_, the glyph's ink equals the accompanying text's cap-height band at **any** text size: re-measured overshoot is exactly **0.00 top / 0.00 bottom**, with no magic offset anywhere. The fixed steps (`sm`/`md`/`lg`) stay correct for standalone glyphs, which have no baseline to answer to. `sizing.test.ts` asserts the step resolves to the relative primitive so the ramp exemption stays deliberate; `Button.test.tsx` asserts the button overrides a caller-supplied step.

### F-023 — `AppShell` has no narrow-container behaviour: the shell overflows on a phone

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-ui` `AppShell` (+ Meridian's use of it) · **Severity:** gap · **Status:** ✅ resolved 2026-08-02 (`Drawer` + `sidebarVariant`)
- Found while answering an owner question about resolved type and padding at 1920×1080 vs 390×844. At 390px the document scrolls to **442px** (52px of horizontal overflow): `AppShell` composes its body as a fixed grid — `bodyColumns()` emits `<sidebarWidth> minmax(0, 1fr)` — with no breakpoint, container query, or prop that lets the start sidebar collapse. The rail keeps its full width, the main region cannot shrink below its content, and the shell pushes past the viewport. Every page inherits it, so the Meridian screens are unusable on a phone even though each individual component behaves correctly at that size (measured in isolation: the Button resolves 14px/48px-tall and stays inside its box).
- **Not worked around:** the honest fix is a component affordance, not app CSS, and the shape of it is a design decision (drawer over the content · off-canvas with a trigger · sidebar stacking above the main region · a `collapseBelow` prop vs. an automatic container query). Picking one without the owner would bake an IA decision into a layout primitive.
- **Backlog:** decide the narrow-container contract for `AppShell` and implement it; then re-run the visual ritual at a phone viewport, which the ritual does not currently require — S2 shipped with desktop-only verification (BLUEPRINT's ritual says "light and dark", never "narrow and wide").
- **Resolved 2026-08-02, and reading the references first moved the root cause.** This entry blamed the fixed grid. The deeper fact is that **fsl-ui had no `Drawer`** — the primitive all three reference systems reach for when a persistent panel has nowhere to live — so no shell could answer a narrow viewport regardless of how its grid was written. Chakra ships **no** AppShell at all (`Drawer`, `Splitter`, `Sticky`, `Container`, `Grid`, `SkipNav`, composed by the app); Spectrum S2 ships no shell either; MUI is the one that names the axis, and it puts it **on the Drawer** — `permanent | persistent | temporary`. The convergent lesson: the variation belongs to the _navigation region_, not to the shell.
- **What shipped.** `Drawer` as an Overlay composite (edge placement, the shared panel-width scale, its own `Dialog` for the accessible name, RAC focus containment and dismissal), and `AppShell` gains `sidebarVariant='permanent'|'temporary'`. `temporary` drops the sidebar track and moves the panel into a `Drawer`, reached from a trigger the shell places at the inline start of the header row — forcing a header row into existence when the host passed none, because otherwise the trigger has nowhere to live.
- **The IA decision this entry refused to bake is still not baked.** Which variant applies is the _app's_ call, exactly where both references leave it: the app knows its breakpoints, the shell does not. What changed is that the choice is now expressible.
- **One safety rule, and it is asserted:** `temporary` needs `sidebarTriggerLabel`, and without it the shell stays permanent rather than hide the panel. An unreachable sidebar is worse than the horizontal scroll this entry measured, and an English default is forbidden (ADR-001).
- **Still open, split out rather than absorbed:** the app has no in-system way to _pick_ the variant — both references ship a breakpoint helper (`useMediaQuery`, `useBreakpointValue`) and we ship none, so an app hand-rolls `matchMedia`. Filed as F-041.
- **Note on scope:** this is _not_ a defect in the P3 aesthetic work. Type and spacing resolve sensibly at 390px on their own (heading 28→20px, body 18→16px, control text 16→14px, `hit` 32→48px via the coarse-pointer override). What is missing is the shell's response to the width.

### F-024 — no transparent resting fill, so the "quiet" toolbar posture is approximated

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-theme` `semantic.colors.action.muted.background.default` · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-04 (fsl-ui ADR-030 — the surface contract)
- Found while shipping `ActionButton` (P3 Slice 4 ②). The reference system's quiet action button has a genuinely **transparent** resting fill, so it borrows whatever surface it sits on and only materialises on hover. Our closest posture is `action.muted`, whose resting background is `neutral.0` — indistinguishable on a white page, but a visible white patch on a tinted or raised surface (a toolbar inside a `raised` Surface, a row action on a selected row).
- **Workaround:** `ActionButton evaluation="muted"` is documented as the quiet posture and reads correctly on the page surface, which is where the Studio uses it.
- **Not fixed yet, deliberately:** making `muted`'s resting fill transparent is a theme-wide colour decision — it also changes `Button evaluation="muted"` — and the evidence rule wants a real consumer first. `ActionMenu` (queue item ⑤, an icon-only trigger that must sit invisibly in a toolbar) is the consumer that will force it; that is the moment to decide, together with whether the contrast suite can reason about a transparent token at all (it currently compares hex pairs, and `transparent` has no hex).
- **Backlog:** decide via governance when ActionMenu lands: either a `transparent` core primitive that the contrast tests treat as "inherits the adjacent surface", or a dedicated `quiet` role in the action tree.
- **Measured when ActionMenu landed (2026-07-25), and the picture is sharper than the original entry:** the quiet rung is not broadly wrong — it is wrong on exactly one class of surface, and only in dark mode. `action.muted.background.default` resolves to `neutral.0` / `neutral.900`, which is **byte-identical to the page and to the Menu popover** in both modes, so a quiet trigger on the page and a quiet row inside a menu borrow their surface correctly. The break is a _raised_ surface: in dark, a `muted` trigger paints `#161616` on a `#262626` card — a visibly darker patch (see the `Overlay/ActionMenu` → `Emphasis` story, dark). In light both the page and the raised surface are `#ffffff`, so the same code accidentally looks right; that coincidence is why this went unnoticed.
- **A third option, better than the two above:** **omit** `action.muted.background.default` rather than introduce a `transparent` value. A component that reads an absent token paints no background, so the row/trigger borrows whatever is underneath, on any surface, in any mode — and the theme still never contains the literal `transparent`, keeping ADR-015's premise that every declared colour is an auditable hex. `extractBorderBackgroundPairs` already skips pairs whose background is absent, so the suite stays green by construction. **Its cost, which is why this is still a decision and not a change:** the pair that vanishes is also the one that proves `muted`'s _ink_ is legible. Today `muted.text.default` is audited against `muted.background.default`, i.e. against the page — remove the background and that audit disappears with it, so it would have to be replaced by an explicit "quiet ink vs page surface" assertion. Governance-sized because it also changes `Button`/`ActionButton`/`ToggleButton`'s quiet rung everywhere at once.
- **Explained 2026-07-29 by reading the model, which narrows the options to one.** The tokens are behaving exactly as written: in dark, `action.muted.background.default` and `informational.primary.background.default` are **both** `neutral.900` — identical by design, because `colors.md` → "Stacking informational surfaces" has the page and every contained surface resolve from the _same_ background token and pays the visual step in `elevation.tonal.*` instead (`raised` = `neutral.800` in dark). So a raised surface's effective colour is **background + tonal overlay**, a composite **no colour token names or can name** — the overlay is produced by the cascade. A quiet control painting the background token therefore paints _underneath_ the lift and undoes it. That is the whole of `#161616` on `#262626`.
- **Consequence for the decision:** the `transparent` primitive is ruled out by `colors.md` (and by `model.md` §5 — names express meaning, not appearance), and "keep painting" cannot be narrowed to the page stratum because `MenuItem` rows live inside an overlay. **Omitting `action.muted.background.*` is the only shape that keeps both written rules**, and it keeps the hex-auditability premise intact: nothing gains a non-hex value, a token simply stops existing, and the child inherits the composite its parent already produced. Its cost is now verified rather than assumed — both contrast extractors `continue` when a counterpart is absent, so deleting the background **silently drops** the `muted.text ↔ background` and `muted.border ↔ background` pairs while the suite stays green. The replacement assertion ("quiet ink vs each `informational` stratum") is the price, and it is the more honest question anyway.
- **Not blocking `ActionMenu`:** its trigger defaults to `secondary` (a quiet fill, matching the reference, which also makes `isQuiet` opt-in), so the shipped default is correct on every surface. `muted` stays documented as the quiet posture with the caveat above.
- **Re-measured 2026-08-02, when F-027 cleared its stated prerequisite — and the entry stays open, because the _other_ condition has not fired.** The ruling says the lawful fix builds only when a real surface demands it, and none does: scanning the Studio for a `muted` control inside a `Surface level="raised|overlay|blocking"` returns **nothing**. The two `muted` controls it renders sit on the page stratum.
- **Why the class is narrower than it looks, now measurable rather than argued.** Only `Surface` applies `elevation.tonal.*`; every overlay in the package — `DialogModal`, `Menu`'s popover, the new `Drawer` — paints `informational.*.background.default` and carries its depth in the shadow instead. So a quiet control matches its surroundings on the page **and inside every overlay we ship**, and the mismatch needs the one arrangement nobody has built yet: a quiet control inside a raised card, in dark.
- **Owner ruling 2026-07-29 — a component always paints.** The question this entry carried ("can a component paint nothing?") is closed the other way from the recommendation above: the system follows `colors.md`'s doctrine to the letter — no `transparent` primitive **and no omitted background either**. Every semantic background stays a declared, auditable value, and the `muted.text ↔ background` / `muted.border ↔ background` contrast pairs stay in the suite instead of needing a replacement assertion. **What this ruling costs, stated rather than hidden:** the dark-raised mismatch above (`#161616` on `#262626`) is accepted as a known limit of the quiet rung — a quiet control is correct on the page and inside a Menu popover (byte-identical backgrounds, both modes) and visibly darker on a raised dark surface. The lawful future fix inside this ruling is a stratum-aware opaque value (governance-sized, F-027's dark audit is its prerequisite), not omission; nothing builds until a real surface demands it (evidence rule). Severity stays paper-cut; the _decision_ half of this entry is done.

- **The consumer condition fired 2026-08-03, found by looking rather than by scanning (F-029's browser verification).** The 2026-08-02 re-measure scanned for a `muted` control inside a `Surface level="raised|overlay|blocking"` and found none, so the entry stayed parked. That scan asked the wrong question: `Surface` is not the only thing that raises a stratum. Measured in Chromium at 1280px, dark, on the Studio's Team page — the quiet `Remove` in each table row paints `rgb(22,22,22)` while its `table/item` row resolves `rgb(61,61,61)`, so **every row shows a black pill where the reference shows nothing**. Wider than the `#161616`-on-`#262626` case this entry predicted, and it was on screen the whole time; the light mode hides it because the page and the row are both white. Verified pre-existing by measuring the same button on `e26dde2`: byte-identical fill, so F-029's ink change neither caused it nor worsened it.
- **What that unblocks.** Both conditions the owner ruling set are now met — F-027 cleared the prerequisite, and a real surface demands it. The lawful fix inside the ruling is unchanged: a **stratum-aware opaque value** for `action.muted.background.*`, not `transparent` and not an omission. What is new is that it now has a measured consumer to be designed against, and a second one to check while doing it (the same row's `Badge`).

- **Resolved 2026-08-04 (ADR-030), and the ruling survived intact — what moved was where the value lives.** The ruling's own fix — "a stratum-aware opaque value" — cannot be minted in the theme, by the theme's own doctrine: this entry's 2026-07-29 analysis proved the effective surface is a composite _no colour token names or can name_ (background + tonal lift, produced by the cascade), and the consumer that fired shows the class is wider than tonal strata anyway — the table row paints `input.primary`, another family's fill. Only the painter knows the surface. So the painter publishes it: hosting surfaces spread `publishSurface(fill)` (the fill plus `--fsl-surface`), and the quiet rung's resting fill/edge read `var(--fsl-surface, <own token>)`. Outside a publisher, byte-identical to before (the fallback is the old value); inside one, the control borrows the real composite — always one of the opaque values the theme audits. Nothing transparent, nothing omitted, every theme pair intact: the ruling, kept to the letter.
- **The bounds came from measurement, and one of them from a seeded failure.** Resting state only (the engaged fills are how a quiet control materialises); the muted rung only (every other fill is a voice); **selection fills never publish** — a selected row inverts to near-white in dark, where the muted ink measures 1.5–1.8:1, seeded into the new inventory entry as a mutation and caught, so a quiet control on a selected row keeps its own legible pill (today's exact rendering); **a publisher's transient states do not republish** — the first draft had rows publish whatever fill the render resolved, and the inventory killed it: the dark row _hover_ fill measures 2.65:1 against the destructive ink, so a row paints its resolved fill and publishes its resting one; **voiced fills never publish** — a toast's red and a Menu's or Box's non-primary informational fill are voices (the dark muted fill fails the destructive ink's floor), so only the page-like `primary` voice and Surface's tonal strata publish. `--fsl-surface` sits in the §7 host-facing namespace on purpose: a host that paints its own panel publishes the same property and gets the same behaviour.
- **Guarded on both sides.** fsl-theme's cross-role inventory gains `quiet control on published surfaces` — the quiet ink against every publishable surface (the informational strata and the row family's resting fill), at the rung's own floor, per bundle and per mode — and the destructive-ink entry gains the row surface, since a destructive row action now rests on it. fsl-ui's `surfaceScope` suite pins both halves: publishers publish exactly what they painted; consumers read the var at rest and their own fills everywhere else.
- **The Badge beside it was checked and left alone, deliberately.** A chip paints its own fill _as its voice_ — that is the descriptive chip's whole design (F-010). It is not a quiet part and does not follow the surface.
- **Lesson:** "the theme should own this value" and "the theme _can_ express this value" are different claims. When the system's own docs prove a value is a cascade product, the lawful home for awareness of it is the layer that runs the cascade — with the theme still owning every value that can land there, which is what the inventory pairs.

### F-025 — two emphasis roles rendered byte-identically, in two different contexts

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-theme` `semantic.colors.action.muted` / `input.secondary` (dark alternate) · **Severity:** gap · **Status:** ✅ fixed (same day)
- Owner observation while reviewing the buttons: "`secondary` looks like `muted` and `muted` looks like `secondary`". Measuring the resolved values showed two separate problems. **(a) Light mode — the ladder changed mechanism mid-way:** `primary` and `secondary` carry fills, but `muted` carried a _visible border_ over a white fill, which is precisely the outlined-secondary idiom of Material/Bootstrap. Two adjacent rungs distinguished by a different _kind_ of cue cannot be ordered by eye. **(b) Dark mode — an outright collapse:** `action.secondary` and `action.muted` resolved to the identical triple (`neutral.700` / `neutral.500` / `neutral.50`). Two documented emphasis levels, the same pixels.
- **The invariant that made the fix obvious:** the theme never uses `transparent` — not once. Where `baseTheme`'s own comments say "transparent bg" the value is `neutral.0` (light) or `neutral.900` (dark): **the surface's own colour**. Every semantic background is opaque _by design_, because that is what makes contrast auditable at the token level (ADR-015 compares hex pairs). A `transparent` ghost rung — the first thing this review proposed — would have broken it. The aligned answer was the system's existing idiom.
- **Action:** `action.muted` now mirrors its background in the `border` dimension for every state (`focused` excepted — the one state that must show on any surface), so it has no edge at rest and materialises on hover; in dark it sits **on** the canvas (`neutral.900`) instead of one stratum above it, with dimmer resting ink (`neutral.300`, 13.6:1). The ladder is now one mechanism — solid fill → light fill → surface — monotonic in both modes. `ConfirmationDialog`'s cancel and `Wizard`'s back moved `muted` → `secondary`: a flow-critical alternative needs a visible affordance, and the quiet rung is now genuinely quiet.
- **Guard:** new invariant "roles within a context are distinguishable" (`colors.test.ts`) — for every ux context and both modes, no two roles may share a resting `(background, border, text)` triple. It asserts _difference_, not degree, so it stays a defect check rather than an aesthetic one. **It immediately caught a second, unrelated collapse the review had not looked for:** `input.primary` and `input.secondary` were also identical in dark mode (the dark override had dropped the lighter-border distinction the light mode uses to make secondary recede). Fixed the same way — secondary's border mirrors its own fill until hovered.
- **Lesson:** the existing distinguishability suite compared _states within a role_ and nobody had compared _roles within a context_. A vocabulary can collapse along any axis it does not test.

### F-026 — `SearchField` names the same `data-part` twice, so its anatomy is ambiguous

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-ui` `SearchFieldControl` · **Severity:** paper-cut · **Status:** resolved 2026-07-29 (forms item D)
- Found while measuring `ActionButton` against the field row: `[data-scope="search-field"][data-part="control"]` matched **two** nested elements — the positioning wrapper and the `<input>` inside it. `querySelector` returned the wrapper, which reports `padding: 0` and `border-radius: 0` (the frame lives on the input), so the measurement read as a defect until the DOM was inspected. The resolved geometry is in fact correct: the input lands on the same 34px field row as `TextField` and `Select`.
- **Why it matters beyond the paper cut:** `data-part` is the published anatomy (CONTRACT §2) — the handle host CSS, tests, and Studio inspection all bind to. A duplicated name makes the contract non-addressable: a host that styles the "control" cannot say which box it means, and every future measurement hits the same trap.
- **Not fixed here:** naming the wrapper is a contract change (`frame`? `adornmentLayer`? or hoist the frame onto the wrapper and leave the input unstyled), it needs the same treatment for any other composite that wraps a field in a positioning layer, and it belongs with the field-row work rather than mid-way through the Action queue.
- **Resolved 2026-07-29 (forms item D).** The name is `frame`, the precedent C1 set on `ComboBox`: the wrapper paints and `control` stays on the element a caller operates. Both remaining offenders — `SearchField` and `NumberField` — took it, and `KNOWN_NESTED_PAIRS` in invariant #12 is now **empty**. The backlog's own ordering is what closed it: the class guard shipped first, so the two instances could not be quietly left as standing exemptions, and the anti-stale companion forced the list to shrink rather than rot.
- **It was never only a naming fix, which is why it waited for the field-row work.** Both components were hand-rolling the field chrome — `SearchField` declared its own border, radius, motion, state cascade and focus ring, and reserved adornment room with a `calc(icon.md + inset.control.md * 2)` inline padding (48px each side, measured) while absolutely positioning the glyph and button over it. Taking the anatomy's frame/value split deleted all of that: the frame is a flex row, so adornments occupy space by being in it. `NumberField` lost three private style helpers the same way.
- **Two defects fell out of the rewrite, neither of them in this entry.** The clear button rendered on an **empty** field, because React Aria publishes emptiness as `data-empty` for CSS and this package ships none — while a comment asserted "the clear button is managed by React Aria (hidden while empty)", and a test pinned the wrong behaviour by counting two glyphs. And the trailing 48px stayed reserved whether or not the button was there.

### F-027 — the border-contrast inventory audits the light bundle only; the dark alternate is unguarded

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-theme` `colors.test.ts` › "Color contrast — border vs background" · **Severity:** gap · **Status:** ✅ fixed (2026-07-31 — P3 Round 0, ADR-024)
- Found while fixing the dark `action.secondary` divergence (see the ROADMAP's Slice 4 ② entry). That guard is the theme's strongest colour invariant — an explicit inventory of every `{ux}.{role}.{state}` whose border sits below 3:1 against its own fill, so any delta in either direction fails the suite and forces a decision. It iterates `bundleEntries` but reads only `entry.base`: `entry.alt` is extracted in the fixtures and never compared. Both light bundles are audited (default + bruttal); **neither dark alternate is.**
- **Consequence, measured:** the dark alternate currently has ~90 sub-threshold contexts per bundle, none of them decided — the divergence this review fixed (a dark secondary action reading exactly like a text input) lived there precisely because nothing was watching. Dark is where these mistakes are easiest to make: the alternate remaps references by hand, so a pattern the light mode expresses one way can silently be expressed another.
- **Not fixed here, deliberately:** the inventory's value is that every entry was _decided_ — pasting 90 names in to make a new test green would produce a guard that documents nothing and locks in whatever dark happens to be today. Each dark context needs the same pattern classification (a)–(d) the light inventory carries.
- **Backlog:** audit the dark alternate context by context, build `KNOWN_BORDER_CONTRAST_VIOLATIONS_ALT` (per bundle, same shape as the `knownBorderViolations` override bruttal already uses), then extend the test to iterate `[base, alt]`. Governance-sized: it is a colour review of the whole dark surface, not a test edit.
- **Resolved 2026-07-31 (P3 Round 0, ADR-024), and the audit came in far smaller than the backlog sized it.** The ~90 contexts are not 90 decisions: two mechanical rules — `disabled` is exempt (WCAG 2.2 §1.4.3, which the text pairing beside it always assumed) and a border that resolves to its own background has no edge by construction — account for roughly two thirds, and what survives sorts into the same three patterns in both modes. Classifying the remainder turned **22 dark-only contexts into four omissions**: the alternate had remapped a role's `background` subtree and left its `border` subtree at base values, so the base's interaction direction (darken the edge, correct against a light fill) survived onto the dark canvas, where it weakens instead. The four are recorded in the ROADMAP's Round 0 entry with their remaps.
- **What the split changed beyond adding a mode.** The old inventory could not distinguish a role that mirrors its background from one with a soft edge — both are "below AA Large", so a role _losing_ its edge produced no delta. That case is now a failure in either direction, and it is the case that let F-025 ship. Verified by mutation rather than by assumption: reverting each remap, and separately un-mirroring a filled role while keeping it under the threshold, each fail the suite.
- **Not fixed, deliberately, and now visible:** the brand-step cascade for engaged and marked edges in dark → F-039. The audit's line was that applying a decision the alternate had already made elsewhere is a fix, while choosing a new ramp step is the owner's.

### F-028 — `Toolbar` claims `role="toolbar"` but is not a single tab stop

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-ui` `Toolbar` (React Aria `useToolbar`) · **Severity:** gap · **Status:** ✅ resolved 2026-08-05 (owner-delegated ruling) — the claim was correct, the removal answered the wrong question
- Found while writing the keyboard test for the realigned Toolbar (P3 Slice 4 ④). The old suite asserted only that the arrow keys move focus, which passes; the new suite additionally asserted that Tab _leaves_ the cluster, and it failed — Tab steps to the next control inside the toolbar. Reading `react-aria-components/private/Toolbar.mjs` confirms why: it renders a div with `role="toolbar"` and `useToolbar`'s arrow-key handler, and nothing manages the children's `tabindex`. It cannot, for arbitrary children.
- **Consequence:** WAI-ARIA APG's toolbar pattern asks for one tab stop with arrow-key navigation inside, so a keyboard user meets a promise the role makes and the implementation does not keep: on a ten-control bar, Tab visits ten stops. The role still buys the named region and the arrow keys, so this is an incomplete pattern rather than a broken one.
- **Not worked around:** the honest fix is to own the roving-tabindex model — track the current child, set `tabIndex` on each, restore focus on re-entry — and our `ActionTriggerGroupProvider` context could carry it for fsl-ui triggers. But a toolbar lawfully hosts a `Select` or any host-supplied control, and those would not participate, producing a _partial_ roving model that is worse than a consistent one. Half an a11y mechanism is not an improvement.
- **What was done instead:** the claim was removed everywhere it appeared. The JSDoc, the ADR-013 rationale and the ROADMAP entry all said "one tab stop with roving focus" — written from the APG pattern rather than from the code — and now describe what the component actually does: a named region with arrow-key navigation, every control its own tab stop. A test asserts the current behaviour so the gap cannot be silently "fixed" in prose again.
- **Backlog:** either upstream (React Aria adds roving tabindex to `useToolbar`, the preferred outcome — it owns the keyboard model) or a deliberate decision to own it here for _all_ children via a documented context every focusable fsl-ui component reads. Governance-sized; it changes the focus contract of every control that can sit in a bar.
- **Lesson:** the previous suite tested the feature that existed (arrow keys) and the docs described the pattern the role implies. Neither was wrong on its own; the gap lived in between. When a component adopts an ARIA role, test the role's _whole_ contract — the parts that fail are the documentation you owe the reader.

- **Resolved 2026-08-05 by owner delegation, and the backlog's "preferred outcome" had already happened — the July analysis just could not see it.** The owner-delegated question this time was not "own a partial roving-tabindex model for fsl-ui triggers only" (correctly rejected in July — a host `Select` would not participate) but a different one raised independently: does react-aria's `useToolbar` already move focus generically, without child cooperation, the way a DOM-query roving-tabindex utility would? Read `react-aria`'s `private/toolbar/useToolbar.js` (the installed `~1.19.0`/`3.50.0` pin) line by line: on a `Tab` keydown it does **not** touch any child's `tabindex` — but it calls `createFocusManager(ref)`, whose `focusFirst`/`focusLast` walk the DOM with a generic `TreeWalker` (`getFocusableTreeWalker`, filtered to tabbable elements — no registration, no context, no cooperation required from what it finds) to move focus itself to the toolbar's first-or-last tabbable descendant, then returns without calling `preventDefault`, leaving the keydown's native default Tab action to carry focus onward from that new position. The net effect, in one keystroke: Tab from _anywhere_ inside jumps to the boundary and out; Shift+Tab jumps to the boundary and out the other side; arrow keys are unaffected (they do call `stopPropagation`/`preventDefault`, a separate branch). This is exactly a DOM-query roving-tabindex mechanism's payoff — the piece the July analysis assumed did not exist — implemented without a tabindex axis at all.
- **Why the 2026-07-25 test saw the opposite of what ships.** `@testing-library/user-event`'s `tab()` (`convenience/tab.js` → `event/behavior/keydown.js`) computes its Tab destination as `getTabDestination(target, shiftKey)`, where `target` is the element the keydown event was dispatched _to_ — captured before any handler runs — not `document.activeElement` read _after_ the toolbar's own `onKeyDownCapture` has already called `.focus()` on a different element. So user-event's simulated "default action" never sees the jump; it computes "next tabbable after the original target" as if the toolbar had done nothing, which is indistinguishable from the no-mechanism case the July test correctly identified for a _different_, older assumption. This is a testing-library/jsdom simulation gap, not a product defect — confirmed by writing the same assertion with `fireEvent.keyDown` instead (bypasses user-event's destination computation entirely) and watching focus land on the correct boundary descendant, then confirmed a second way in a real engine.
- **Verified in a real browser, not assumed.** Added a `KeyboardInvestigation` story (`docs/fsl-storybook/stories/structure/Toolbar.stories.tsx`) mixing an fsl-ui `ActionButton`, a plain host `<button>`, an fsl-ui `Select`, and an fsl-ui `ToggleButton`, with focusable siblings before/after the toolbar. Driven with Playwright against Chromium (`/opt/pw-browsers/chromium-1194`): Tab from outside enters on the first control; Tab from _any_ control inside (first, middle, or the plain host `<button>`) exits to the sibling after the toolbar in one step; Shift+Tab from a middle control exits to the sibling before it in one step; ArrowRight from the first control walks every child in DOM order — `ActionButton` → the plain `<button>` → the `Select` trigger → the `ToggleButton` — with no gaps and no cooperation required from the non-fsl-ui button. All four checks the task specified (Tab-in-once, Tab-out-once from mid-toolbar, arrow keys including host children, no regression for fsl-ui-only toolbars) came back positive.
- **What shipped: no code change to `Toolbar.tsx`, because none was needed.** The fix is to the record, not the component: `Toolbar`'s JSDoc, ADR-014's rationale in `CONTRIBUTING.md`, and this ROADMAP's Slice 4 ④ entry all say again that the toolbar is a single tab stop with arrow-key navigation inside — this time with the mechanism named rather than asserted from the pattern. `Toolbar.test.tsx`'s "documented gap" test is replaced by a `fireEvent.keyDown`-driven test asserting the actual jump (including the plain host `<button>` and `Select` case) and a `user-event` arrow-key test extended to cross the same host `<button>`; both pass, and the suite stays 100% statements/branches/functions/lines (2777/2777, no new source lines to cover). Existing fsl-ui-only toolbars (`ActionTriggerGroupProvider` consumers) are unaffected — nothing in their code path changed, and their own tests still pass.
- **Lesson, layered on the first one.** The 2026-07-25 entry's lesson — test the role's whole contract — was right and is why the gap was found at all. What it missed is a corollary: when a jsdom-based test contradicts a documented, deliberately-written-down claim about a _third-party_ dependency's behaviour, the test is not automatically the ground truth — jsdom and its simulation layers approximate the browser, and `user-event`'s Tab simulation is a known-narrow approximation (it does not re-read focus state after a keydown handler moves it). The library's own source was the tie-breaker here, and a real engine settled it.

### F-029 — a destructive menu row has no way to look destructive

- **Date:** 2026-07-25 · **Surface:** `@ttoss/fsl-ui` `MenuItem` / `@ttoss/fsl-theme` `semantic.colors.action.negative` · **Severity:** gap · **Status:** ✅ resolved 2026-08-03 (fsl-ui ADR-028), with the 2026-08-02 recommendation retracted
- Found while building `ActionMenu` (P3 Slice 4 ⑤). A "Delete" row in an overflow menu should read as dangerous, and the two tools available both miss: `consequence="destructive"` is the _semantic_ marker (it drives mechanism — `ConfirmationDialog`'s arming — and deliberately carries no colour), while `evaluation="negative"` fills the **entire row** with `red.600` and white ink, because `action.negative` is a filled destructive _button_. A menu of one red block among grey rows reads as a banner, not as a row.
- **What is actually missing:** the Action colour tree has no "negative **ink** on a surface" rung. `action.negative.text.default` is `neutral.0` — correct on a red fill, useless on a menu surface. The valence exists as ink elsewhere (`informational.negative.text`), but a `MenuItem` is Action and CONTRACT §1 forbids reading another entity's row, so it cannot reach it.
- **Workaround shipped:** the destructive row uses `consequence="destructive"` alone and looks like its siblings. Honest, and the confirm mechanism still fires; it just does not warn visually.
- **Why not fixed here:** every fix touches the colour model. (a) A new evaluation (`negativeQuiet`) grows the taxonomy for one component and reads as a variant axis we do not have. (b) Making `action.negative` ink-based breaks the destructive `Button`, which must stay a filled red command. (c) Letting Action read `informational.negative.text` punches a hole in §1's one-row rule. (d) Adding a `text`-only sibling role (e.g. `action.negative.textOnSurface`) is probably the least-bad shape, and it is exactly the kind of vocabulary addition the ADR workflow exists for.
- **Backlog:** pick between (a)–(d) with the owner. Worth pairing with F-024: both are the same underlying question — how the action tree expresses "this control borrows its surface instead of painting one".

- **Narrowed 2026-08-02 by reading the docs, and the answer is smaller and harder than the four shapes suggested.** Two of them are **forbidden outright**: `colors.md` › Picking a role states that intensity within a valence is expressed by `dimension`, never by combining valence with emphasis (`action.negative.muted` is the ❌ example's shape). So the space is not four options wide.
- **What the model promises, and what the theme actually holds.** The doc's ladder is `negative.background` loud / `negative.text` quiet. Measured, `action.negative.text.default` and `feedback.negative.text.default` are both `#ffffff` in both modes — the label _on the filled red_, not a standalone ink. The quiet rung the model describes exists in `input.negative.text` and `informational.negative.text` (which is what F-032 moved the validation message onto) and, in `action`/`feedback`, was taken by the filled variant. So the entry's premise was right about the outcome and wrong about the cause: **the Action tree does not lack the rung — the loud variant occupies it**, and all three dimensions of `action.negative` are load-bearing for the filled destructive button.
- **The cross-role route does not apply here, on a closer read.** F-036's pairing is one ux's ink (`input.negative.text`) on **another role's surface**; the ink stays inside the component's own ux. A destructive `MenuItem` is `Action` (verified: `menuItemMeta.entity`, reading `vars.colors.action[evaluation]`), so borrowing `informational.negative.text` would be cross-**ux** ink, which has no precedent and no rule permitting it.
- **Conclusion: not buildable under the current registry, and the doc now says so.** `colors.md` › Picking a role gained the paragraph naming where the loudness ladder exists and where it does not, so the next reader does not rediscover this by shipping white text on a page. Closing it means a `dimension`-registry change — governance, per Rules of Engagement #3 — and is not a theme value tune.

- **Governance proposal 2026-08-02**, written in the format `governance.md` § Review asks for, because this is a model change and the process routes it through design review rather than through a slice.

  **What problem exists.** No lawful expression for a quiet destructive action — a menu row, a destructive link, a text-only "Delete". `MenuItem` is Action; `evaluation="negative"` fills the row red; `consequence="destructive"` carries no colour.

  **Why reuse is not enough.** Measured, not assumed: `action.negative.text` and `feedback.negative.text` are `#ffffff` in both modes, because P3 chose deep filled valences (ADR-021) and the `text` dimension became the label _on that fill_. The doc's loudness ladder — "intensity within a valence is expressed by `dimension`, `negative.background` louder than `negative.text`" — therefore has no quiet rung in these two contexts, while it works in `input`/`informational` whose valence fill is light enough for one ink to serve both. Combining valence with emphasis is forbidden outright. Borrowing `informational.negative.text` would be cross-**ux** ink from an Action part, which has no precedent: F-036's pairing is one ux's ink on another **role's surface**, and the ink never leaves its own ux.

  **What is being proposed.** Recognise that a filled surface's label is not the role's `text` — it is **static ink on a coloured fill**, a different concept, and the reference already models it that way (Spectrum keeps `static-*` apart from its semantic content colours). Moving those labels off `{ux}.{valence}.text` frees the dimension to be the standalone valence ink the model already promises, which makes a quiet destructive row fall out with no new axis, no new role, and no rule bent.

  **What impact exists.** Large but coherent: every filled Action and Feedback surface's label moves — not only `negative`, since `action.primary`/`accent` labels sit on fills too. It is a published-surface change and belongs at the same version boundary as the other P3 members. Contrast pairing gains a case the suite does not model today (static ink against every fill it can land on), which is the same shape as the cross-role inventory F-036 added.

  **Alternatives costed.** (a) `consequence` gains a colour projection — defensible from the Lexicon, which already separates `negative` (evaluation) from `destructive` (consequence), but it adds a fourth axis to a three-axis token path for one case. (b) Let a part that paints no surface read the ux it renders _into_ — small, but it weakens the entity→ux alignment the contract test enforces. (c) Do nothing and rule that destructive rows are filled — honest, and visibly worse than every reference.

  **Recommendation:** the static-ink split, taken at the version boundary rather than now. Nothing about it is urgent, and the analysis is what was missing rather than the code.

- **RECOMMENDATION RETRACTED 2026-08-03 — the static-ink split is circular.** Stress-tested against a second theme before building it, and it does not survive: "static ink on a coloured fill" is only static while the fill is known. `action.primary`'s fill is `neutral.1000` in light and `neutral.0` in dark; `action.accent`'s is `brand.500`; `feedback.caution`'s is a yellow. One `static.text` cannot serve all of them, so the split immediately needs **per-role on-fill ink** — which is a token per role per mode holding the label for that role's fill. That is `{ux}.{role}.text` again, renamed. The proposal moved the problem one identifier to the left and cost every published Action and Feedback label to do it. Costed alternative (c) — "do nothing, destructive rows are filled" — was the honest fallback if nothing else worked, and something else did.

- **What shipped instead: alternatives (a) and (b), together and bounded.** Neither works alone, which is why both were costed as weak. (b) supplies the **licence** — a part that paints no surface of its own takes its ink from the surface it renders on — and that is not a new idea in this system, it is what §3.2's validation message has always done; F-036 built the pairing inventory for it. (a) supplies the **trigger**: when such a part carries a valence, `consequence` is what selects it. The objection to (a) was "a fourth axis on a three-axis token path"; it does not apply, because nothing is added to the path. `informational.negative.text.default` is an existing token, and `consequence` is an existing prop already declared per entity, already emitted as `data-consequence`, already driving `ConfirmationDialog`'s arming. It gains reach, not vocabulary. The objection to (b) — "it weakens the entity→ux alignment the contract test enforces" — was the real one, and it is answered by making the crossing **licensed and singular** rather than ambient: the read lives in one module, and `components.contract.test.tsx` now fails any component that reaches for `informational.negative` by hand or that emits `data-consequence`, paints from `vars.colors.action`, and does not route its ink through that module. Before, `Menu.tsx` would have passed such a read **by coincidence**, because it declares an Overlay part too and the test unions the contexts.

- **Scoped by measurement, not by taste.** The tint applies to the quiet rung only (`colors.md` names `muted` as the idiom for "no fill"), paints `color` only (the quiet rung's border mirrors its background by construction — tinting the edge would invent an outlined-destructive language the system does not have), and **yields** at `disabled`, `active` and `expanded`. That last bound is the one the measurement forced: `action.muted` materialises a real fill on engagement and the theme lifts its _own_ ink to clear it — in the dark alternate the engaged fill is `neutral.500` and the muted ink goes pure white. A fixed valence ink cannot follow that. Measured against both bundles: rest `10.02` light / `9.53` dark, hover `9.43` / `5.72`, and the engaged fill **2.65** — under every floor. So the rule holds where the part paints nothing or nearly nothing and hands the ink back where it does not. `disabled` yields for a different reason: unavailability outranks valence, which is also why WCAG 2.2 §1.4.3 exempts it.

- **Guarded on both sides.** fsl-theme's cross-role inventory gained a `quiet destructive control` entry — the destructive ink against every informational stratum plus the quiet rung's resting and hover fills, at AA Normal, in both bundles and both modes. The excluded states are excluded _because the rule does not reach them_, stated in the entry rather than left as a gap. fsl-ui adds the helper's own bounds suite (every edge of the rule has a case naming why it is closed), component guards on `MenuItem`, `Button` and `ActionButton`, and the contract encoding above. The Studio's Team page is the consumer: the row "Remove" keeps its rung and sets `consequence`, while the confirm inside its dialog stays `evaluation="negative"` — the same page now shows both halves of the distinction.

- **Lesson, and it is about the analysis rather than the code.** The 2026-08-02 pass did the hard part — it proved the loudness ladder has no quiet rung in `action`/`feedback`, and that combining valence with emphasis is forbidden — and then reached for the largest available fix without stress-testing it against a second palette. The two alternatives it costed in one line each were the answer, and what made them work was not new information but **composing** them and **bounding** them. A costed alternative dismissed in a sentence deserves the same measurement as the recommendation.

- **Refined 2026-08-04 (fsl-theme ADR-025 / fsl-ui ADR-029): the ink moved to a cross-cutting token, and the licensed crossing retired.** The owner asked whether a Pareto-dominant shape existed; re-reading the model found it already written. `model.md` §6 names the exact criterion the day-old "unprecedented cross-ux read" satisfies — _a question the principal grammar cannot ask in a single token; a system-wide default no `{ux}` owns_ — and provides the mechanism: a cross-cutting sibling of `colors`, gated by necessity + JSDoc + registration, with `focus.ring.color` as the typed precedent (`TokenRef<semantic.*>`, aliasing another semantic token so mode remaps carry it). The structural analogy is exact: ring and ink both render against the stratum rather than a fill of their own, which is why one system-wide colour serves. So `semantic.consequence.destructive.ink` now exists, aliased to the same source the helper read before — byte-identical resolution in both modes and both bundles, zero visual change — and `resolveConsequenceInk` reads it through CONTRACT §1's cross-cutting table like the ring's colour. What this buys: the entity→ux alignment is back to **zero exceptions**; the contrast inventory pairs the token components actually render (audit survives a theme repointing the alias); a theme can retune the destructive ink without touching validation messages. ADR-028's behaviour, bounds and measurements stand unchanged. **The residual lesson:** the 08-03 analysis rejected "(a) consequence gains a colour projection" for adding an axis to the token path, then shipped (a)'s behaviour with a borrowed address — without checking whether the model already had a sanctioned home for axis-less system-wide defaults. It did, one section over from the rules being quoted. When a fix needs a "licensed exception", search the model for the mechanism that makes the exception unnecessary before licensing it.

### F-030 — a composite sub-part that is itself a root collides with its host's root

- **Date:** 2026-07-26 · **Surface:** `@ttoss/fsl-ui` `Menu` / `MenuItem` vs `CONTRACT.md` §5 · **Severity:** gap · **Status:** ✅ fixed (2026-07-28 — forms round R3)
- **Action (round R3):** `MenuItem.structure` moves `root` → **`control`**, and its `data-part` with it. No taxonomy change was needed: `control` is already legal on Action, and it is the more accurate word besides — ADR-022 settled that `control` names the element the user operates, and a menu row is exactly that. Verified in the browser: three rows resolve `[data-scope="menu"][data-part="control"]`, exactly one element resolves `menu/root` (the popover), and no root nests a root. It **is** a change to a published attribute, taken because the attribute it replaces is the ambiguous one; the alternative — admitting `item` to `ENTITY_STRUCTURE.Action` — grows the vocabulary for one component and is the weaker answer. Invariant #12's known-violations list is down to the two `SearchField`/`NumberField` cases item D removes.
- Found by contract invariant #12 the moment it was written (P3 Slice 5 ⓠ), **not** by the browser audit that preceded it — that audit only probed the field stories, so a defect one family over went unseen. `[data-scope="menu"][data-part="root"]` resolves **both** the popover and every row: §5 has composite sub-parts reuse the host's `data-scope`, while `MenuItem` also declares `structure: 'root'` because it is its own component root. Nothing is mislabelled; the convention and the meta are each correct alone.
- **Why it matters where F-026 matters:** the pair is the package's addressing scheme. A host stylesheet targeting the popover also hits every row, and an agent told to activate a menu item cannot resolve one.
- **Not the same cause as F-026.** There, one component names two nested elements `control` by hand; here, two independently correct declarations collide through the scope-reuse convention. So the fix is a §5 rule, not a component edit — which is why it is filed rather than patched inside a Slice-5 field item.
- **Recorded, not hidden:** it is a named entry in invariant #12's known-violations list, and a companion test asserts every listed violation still reproduces, so it cannot rot into a permanent exemption.
- **Backlog:** decide whether a sub-part that is its own root takes a distinguishing `data-scope` (`menu-item`) or whether §5 gains a rule for the collision. Touches the addressing convention, so it is governance — and it should be settled with F-026's fix so the family ends up with one story.

### F-031 — `invalid` is a dead end in the state cascade: an invalid field stops responding to hover and press

- **Date:** 2026-07-26 · **Surface:** `@ttoss/fsl-theme` `semantic.colors.input.primary.*` + `fsl-ui` `STATE_PRIORITY` / `resolveInteractiveStyle` · **Severity:** paper-cut · **Status:** withdrawn 2026-07-29 — not a defect (owner ruling on hover; focus measured intact on the ring)
- Found while checking whether Spectrum thickens a field's border on invalid (it does not — see the ROADMAP's Slice 5 correction). S2 ships **six** negative border colours — `negative-border-color-{default,hover,down,focus,focus-hover,key-focus}` — i.e. the negative valence keeps its own full interaction cascade, including a distinct colour for _invalid **and** focused_ and for _invalid **and** hovered_.
- **Ours is flat.** `input.primary.border` offers `default / hover / focused / disabled / checked / pressed / invalid` as siblings, and `STATE_PRIORITY` places `isInvalid` **above** `focused`, `active` and `hover`. So `resolveInteractiveStyle` returns `invalid` and the other three never resolve: an invalid field gives no hover feedback, no press feedback, and shows the same red on focus as at rest. The control reads as inert exactly when the user is trying to fix it.
- **Why not fixed in P3 Slice 5:** it is not a value tune. Expressing "invalid **and** hovered" needs either combination states in the token model (a `invalid.hover` sub-tree, which no family has today) or a cascade that composes rather than short-circuits — and `STATE_PRIORITY` is the single source both `CONTRACT.md` §3 and the helper derive from, so changing its semantics changes every entity at once.
- **Not a contrast risk either way:** the resting `invalid` border already passes; this is about missing feedback, not legibility.
- **OWNER RULING 2026-07-29 — hover does not apply while invalid.** So the half of this entry that read as a defect is not one: an invalid field going quiet to the pointer is the intended behaviour, and no combination state (`invalid+hover`) is needed for it. What remains is narrower and already answered by the grammar — `focused`, `active` and `pressed` _do_ apply, and `input.negative.border` already carries all three (`default / active / focused / disabled / indeterminate / pressed / expanded`). The dead end was never the cascade's shape; it was expressing invalidity as a **state** on the neutral role, and a state is a single value by construction. Expressed as the `input.negative` **role** — which `colors.md` prescribes for validity, and which the theme already ships — the cascade exists.
- **CORRECTED 2026-07-29 — and the sentence that stood here was wrong twice over.** It said "switch the field family to the `input.negative` role … and add the `background` rungs that role lacks". Lexicon §10.15 forbids exactly that for the control (see F-032's second correction), and the premise underneath it — that the control needs a richer cascade to signal focus while invalid — does not survive measurement.
- **Measured** (Studio Settings, invalid slug, 1280px): invalid and unfocused → border `rgb(220,38,38)`, no outline. Invalid and keyboard-focused → border **still** `rgb(220,38,38)`, plus a `2px solid rgb(29,62,207)` outline. So focus is not lost while invalid; it is carried by the **ring**, which is a separate declaration (`focusRingOutline`) that the colour cascade never touches. The border keeping the valence and the ring keeping the focus is two signals on two channels, not one signal lost. `active`/`pressed` are moot on a text field. (Measuring gotcha worth recording: the box animates `border-color`, so a snapshot taken immediately after submit catches an intermediate — a first probe here read `rgb(104,116,224)` mid-transition and briefly looked like a defect.)
- **What is left of this entry: nothing.** Every half is either the intended behaviour (hover, by ruling), already signalled on another channel (focus, by the ring), or not applicable (press). Options (a), (b) and (c) below are **withdrawn** — each was a way to give a _state_ a cascade, for a problem that turned out not to exist. The one real defect this entry sat next to was the message's ink, which is F-032.
- ~~**Backlog:** decide between (a) combination states for the valences that need them, (b) a composing cascade where a valence narrows the palette instead of replacing the value, or (c) accepting the flat model and documenting that validation outranks interaction. Governance-sized — it is a change to §3's cascade contract.~~

### F-032 — the validation message reads the neutral role, so it carries no valence

- **Date:** 2026-07-26 · **Surface:** every field's `validationMessage` + `@ttoss/fsl-theme` `input.primary.text.invalid` · **Severity:** gap · **Status:** resolved 2026-07-29 (forms item F)
- Measures the second half of what **F-009** predicted ("`Text` has no negative/danger tone, so the message cannot even be tinted in-system"), on a component where the message part _does_ exist. Found while screenshotting the `Invalid` story during P3 Slice 5 ⓠ.
- **Measured** on `Input/TextField` › `Invalid` at 1280px: the validation message resolves `rgb(22,22,22)` in light and `rgb(255,255,255)` in dark — **byte-identical to the label beside it** — while the control's border carries `rgb(220,38,38)` / `rgb(252,165,165)`. So the error copy is typographically indistinguishable from the hint copy, and the whole valence rests on one border.
- **Not a bug in the component.** `input.primary.text.invalid` is _deliberately_ the control's readable-value colour — the theme's own comment says the value stays readable and the valence lives on the border. The gap is that the envelope has no ink for error copy, so `TextFieldError` reads the only `invalid` text token there is and gets neutral ink.
- **Consequence beyond aesthetics:** with the message unstyled, the invalid state is signalled by border colour alone, which is the WCAG 1.4.1 shape (colour as the only carrier) that the in-control validation glyph is meant to answer.
- **Widened, not re-found (2026-07-28, forms C2):** the same measurement on `Select`'s new message part, in both modes — `rgb(22,22,22)` light, `rgb(255,255,255)` dark, with the trigger border at `rgb(220,38,38)` / `rgb(252,165,165)` byte-identical to `TextField`'s. It is one token's absence reaching every field, which is why the fix is a token and not a component.
- **CORRECTED 2026-07-29 — this entry was filed on a false premise.** It said the envelope "has no ink for error copy". The theme **has** one: `input.negative.text.default` resolves `red.700` in light and `red.300` in dark, with `input.negative` carrying all three dimensions. Nothing reads it. What the components read is `input.primary.text.invalid`, which is correctly the _neutral_ role's readable-value colour — so the defect is a component reading the wrong **role**, not a missing token. Found by reading `families/colors.md` and the theme instead of reasoning from the components; the reading rule this taught is now binding in ROADMAP "Before deciding anything".
- **And the docs already prescribe the fix.** `colors.md` → "Picking a role": _"Valence dominates emphasis: if the token communicates outcome or **validity** … pick the valence first — emphasis is implicit."_ Validity is exactly what invalid reports. The same doc names `input.negative.border.focused` by hand in its focus table. So an invalid field is the `input.negative` **role**, and `input.primary.*.invalid` — a state on the neutral role — is the deviation.
- **CORRECTED AGAIN, and this is the load-bearing one (2026-07-29).** The line that stood here said "switch **the field family** from the `invalid` state on `input.primary` to the `input.negative` role". That is a category mistake, and the Lexicon names it: §10.15 — _"Expressing validation by re-voicing the control (`evaluation: negative`) is a category mistake: state lives in the user's data, evaluation lives in the author's pen. Adjacent display parts (a validationMessage) lawfully carry `negative` valence about the invalid state."_ §7's State table says the same from the other side. The previous correction reached `colors.md` and stopped; `colors.md` ranks roles for _a token that communicates validity_, which the message's ink is and the control's chrome is not. Precedence (`model.md` §11) puts the Lexicon above the family doc, so the scope of "pick the valence" is the reporting part alone.
- **The theme had already written the answer**, in the comment on the very token this entry called wrong: `input.primary.text.invalid` is annotated _"value stays readable in the control; valence text lives on the validationMessage via `input.negative.text.*`"_. Two corrections were spent arriving at a sentence already sitting beside the token.
- **Fixed** by giving `buildFieldTextPartStyle`'s `negative` tone the role instead of the control's colours — one branch, reaching all seven field roots plus `Checkbox` through the shared envelope, exactly as that function's comment predicted it would.
- **Measured after** (Studio Settings, invalid slug, 1280px, both modes, resolved not token): message `rgb(185,28,28)` light / `rgb(252,165,165)` dark against its surface — **6.47:1 and 7.97:1**, both clearing AA 4.5 for its 14px/400 copy. Was `rgb(22,22,22)` / `rgb(255,255,255)`. The control is unchanged and deliberately so: border `rgb(220,38,38)` on fill `rgb(255,255,255)` = 4.83:1, matching the theme's own comment on that token to the hundredth, and the value ink stays the reading ink.
- **Guarded** in `fieldEnvelope.test.tsx` — seven roots assert the message reads the role, one asserts the value does not turn red. A companion test comparing the message to the label was written, seen to **pass with the defect present** (jsdom resolves no custom properties, so both sides are `var(…)` strings that differ by name regardless of value), and deleted rather than kept as reassurance.
- **Left open, split out:** the new pairing is cross-role — negative ink on the page surface — and the theme's contrast suite pairs text only against its own role's background. Tracked as F-036.

### F-033 — a standalone `Checkbox`/`Switch` can turn invalid but cannot say why

- **Date:** 2026-07-26 · **Surface:** `@ttoss/fsl-ui` `Checkbox`, `Switch` · **Severity:** gap · **Status:** ✅ fixed (Checkbox 2026-07-26, forms A2; Switch 2026-07-29, forms E)
- Found answering "can we use the form components individually — say a modal with a confirmation checkbox, or one age input, with field validation?" Probed rather than assumed, and the probe split into a working case and a broken one.
- **Measured.** A required `Checkbox` inside a `Form`, after a failed submit: `aria-invalid="true"` on the input, and its rendered parts are exactly `root, selectionControl, label` — **there is no `validationMessage`**. So the box turns red and the submit is blocked, and nothing on screen states the rule. `Switch` has the same three parts and the same hole. (`Slider` also has no message part, but that is a **boundary rather than a gap**: React Aria gives Slider no `FieldErrorContext`, because a slider always holds a value in range.)
- **Same shape as F-009**, one family over: there, `Select` had nowhere to render a message while its siblings did; here it is the selectables. The single `Checkbox`/`Switch` carry their label as inline children rather than through `LabelContext`, which is why they were never given the envelope in the first place.
- **The other half of the probe, which does work:** a field outside any `Form` validates through a `validate` callback with `validationBehavior="aria"` — measured `aria-invalid="true"` and the message rendered, no form element involved. What does **not** work standalone is a _native_ constraint (`isRequired`, `minValue`, `type="email"`): those need a real submit to fire, and `validationBehavior="aria"` disables them by definition. That is a platform fact, not our choice, and it is worth documenting rather than working around.
- **◐ `Checkbox` fixed 2026-07-26** (forms item A2): it takes `description` and `errorMessage`, matching S2's own vocabulary — whose documented example is this exact terms-and-conditions checkbox. Three things the work established. (a) The supporting copy cannot simply be placed inside the row: **measured**, the accessible name became `"Accept termsYou agree to the terms."` and a name query for the label alone stopped matching, because React Aria computes the name from the label's content and the label _is_ the row. Pinning the name with `aria-labelledby` and linking the copy with `aria-describedby` fixes it, verified in the Studio where the name is now the label alone. (b) React Aria's `FieldError` **cannot** be used here: it returns `null` unless its context reports invalid, and on a lone `Checkbox` that context stays quiet even while the input carries `aria-invalid="true"` and the form refuses to submit — so the message is gated on the render-prop `isInvalid`, the same flag that already tints the label. (c) The parts are **internal** (no meta), following `CheckboxGroup`, which already emits `description`/`validationMessage` that way — so `Selection` did not have to grow a role.
- **✅ `Switch` fixed 2026-07-29** (forms item E), and the verification this entry asked for settled it: read in the RAC 1.19 **source** (`dist/private/Switch.mjs`, not the types alone), plain `Switch` provides neither context — but it is marked `@deprecated: Use SwitchField + SwitchButton instead`, and `SwitchField` provides **both** (`TextContext` with description/errorMessage slots, `FieldErrorContext` with the full validation triple) plus the `isRequired`/`isInvalid`/`validate` props plain `SwitchProps` omits, reading the Form's `validationBehavior` with the `native` default. So the fix was not adding a part — it was moving onto the root the upstream had already built for this exact hole. Our `Switch` now renders `SwitchField` (root, a `<div>`) + `SwitchButton` (the clickable `<label>` row, internal `data-part="button"`), which also dodges the name-absorption trap A2 measured on `Checkbox`: the copy is a _sibling_ of the label, so the shared envelope parts work here as designed — `aria-describedby` wired by the slot, platform constraint copy from the always-mounted `FieldError` — instead of `Checkbox`'s gated re-implementation. Guards: the envelope class guard moved `Switch` from group 3 to group 1 (the removal its own test name asked for), and `Switch.test.tsx` pins a required switch blocking a submit and clearing on toggle. _Studio consumer:_ Settings' "Enforce two-factor authentication" switch, its consequence as a linked description.
- **The `validationBehavior` half of this entry is withdrawn.** It proposed defaulting the prop from Form presence; measurement showed that would be a regression, because `aria` mode does not merely stop displaying native constraints, it **removes** them (`validity.valid` reads `true` on a required field). React Aria's `native` default is already correct in both contexts — see FORMS.md §2b for the five measured cases.

### F-034 — a focused row is marked with a ring; the reference marks it with a background

- **Date:** 2026-07-28 · **Surface:** `@ttoss/fsl-ui` `SelectItem` / `ComboBoxItem` / `MenuItem` / `ListBoxItem` / `GridListItem` · **Severity:** paper-cut · **Status:** ⛔ closed 2026-08-02 — will not do
- Found while unifying the choosable row in forms C4. Every row we ship marks keyboard focus with an `outline` ring, which C4 made consistent and safe — it is now inset by exactly the ring width, so it cannot be clipped at a scrolled list's edge (measured: the focused option in a scrolled `ComboBox` list sits **0.11px** from the viewport edge, and the previous `+2px` ring was cut off; `-1px`, the other inset in use, still clipped there).
- **What the reference does differently:** Spectrum's token set has **no** ring for a menu row. It has `menu-item-background-color-keyboard-focus` alongside `-hover`, `-down` and `-default`, plus `menu-item-background-opacity: 0` — so a focused row is marked by its **fill**, in the same dimension as hover, and the ring is reserved for controls that stand on their own. That is a coherent model: inside a list, focus and hover are the same kind of signal (which row am I on), and a ring drawn inside a 32px row competes with the row's own rounded box.
- **Why not changed here:** it is a change to the focus _language_, not to a row's geometry, and it applies to every row-bearing component at once. It also needs a decision our colour model does not currently have: a `focused` background rung distinct from `hover` in the `input` subtree, so that focus and hover are distinguishable when both are true. `resolveInteractiveStyle` already resolves `focusVisible` for `border`, never for `background`.
- **Backlog:** decide whether a row's focus signal moves from ring to fill, which needs `input.*.background.focused` (or an explicit ruling that hover's value is reused). Related to F-031 — both are questions about which state wins in the cascade rather than about a component.
- **Closed 2026-08-02 as a deliberate no, on the reading F-039 settled.** Required Pairing #3's second clause is **conditional** — "when focus distinction depends on color" — and with the ring drawn on every entity alike it does not: focus is already indicated, at 9.04:1 against the dark canvas. So the docs do not ask for this, and adding `input.*.background.focused` would put a token in the theme that nothing is obliged to read, against the evidence rule.
- **The reference's model is coherent and so is ours; they differ in where the duty sits.** Spectrum has no ring for a menu row, so its fill _must_ carry focus. We draw a ring everywhere, so a focused fill would be reinforcement rather than replacement — a different, smaller proposition than the one this entry was filed for.
- **What the entry got right, and what survives it:** the observation that a ring inside a 32px row competes with the row's own box was real, and it was answered in forms C4 by the shared inset (`FOCUS_RING_INSET`, derived from the ring width so it cannot clip). That is the part that needed doing, and it is done.
- **Readmission criterion**, so this is a decision rather than an abandonment: measured evidence that the ring alone is insufficient in a row — a scrolled list where the inset ring reads ambiguously, or a coarse-pointer surface where it is missed. Then it returns as "add reinforcement", not as "replace the ring".

### F-035 — the control inset is container-fluid, so the field row's height is fluid although its floor is not

- **Date:** 2026-07-28 · **Surface:** `@ttoss/fsl-theme` `spacing.inset.control.*` vs ADR-019's non-fluid ruling · **Severity:** paper-cut · **Status:** ✅ fixed (2026-07-29, owner ruling — ADR-022)
- Found while dismissing a suspected regression in forms C4: a `Select` trigger measured 32.5px at a 900px viewport where every doc says the field row is 34px. It was not a regression — `TextField` measured identically — but the number in the docs is not the number on screen at most widths.
- **Measured** at 390 / 900 / 1280 / 1920: `--tt-spacing-inset-control-sm` is `calc(1 * clamp(4px, 0.25cqi + 3px, 6px))` and resolves **4px / 5.25px / 6px / 6px**, while `--tt-sizing-hit` is `clamp(32px, 2rem, 36px)` and resolves **32px throughout**. So the field row measures **32 / 32.5 / 34 / 34**: its ergonomic floor is rem-anchored exactly as ADR-019 ruled, and its visible height rides a container-fluid inset.
- **This is F-021's shape one property over.** ADR-019/020 ruled that control _geometry_ is not container-fluid; F-021 records that the ruling was never extended to control _type_. The **inset** is geometry by any reading, and it escaped the ruling too — so "control geometry is not fluid" is currently true of the floor and false of the box.
- **Consequence beyond documentation:** a row and a field are meant to differ by exactly the 1px border per edge the field draws (32 vs 34). That holds where content clears the floor and collapses to a 0px difference at 390px, where both bottom out at `hit`. Any invariant phrased in pixels rather than in tokens is therefore viewport-conditional — which is why contract invariants #10, #11 and #13 all assert **token identity** and never a pixel, and that choice is now load-bearing rather than stylistic.
- **Narrowed 2026-07-29 by reading `spacing.md` and the theme: the mechanism already exists and is used twice.** Opting a dimension out of the fluid engine is not "stop being fluid", it is **bounding** — `inset.action.block` is `clamp(8px, {core.spacing.2}, 9px)`, documented as "so a CTA resolves ~40px on the desktop while generic controls stay ~32px", and `separation.interactive.min` is `clamp(8px, {core.spacing.3}, 16px)` for an ergonomic guarantee. `inset.control.sm` is the unbounded `{core.spacing.1}`, which is exactly why the field row drifts. Note the irony: **the sentence justifying the bounded action inset asserts a stable ~32px control height that the unbounded control inset does not deliver.** So the cheapest move consistent with everything written is to bound `inset.control.*` the same way — no new token, no new mechanism, no ADR-019 rewrite, and the doc sentence becomes true. Whether mobile should additionally _grow_ (the reference switches on pointer, and `sizing.md` already rules ergonomics "one value per pointer profile … never fluid") is a second, separable question; note that the type ramp has no `coarse` hook at all today.
- **Backlog:** fold into F-021's ADR (extending ADR-019 from control geometry to the properties that actually determine a control's box). Either the control inset becomes rem-anchored like `hit`, or the ruling is narrowed in writing to "the floor is not fluid" and the docs stop quoting a fixed pixel height. Until then, the doc fix is to state the ramp — tracked as R1 in `INTERNAL/FORMS.md` §4b.
- **Read against the authorities 2026-07-29 (owner asked what our own docs already rule), and the reading sharpened the entry.** (1) The theme **conforms to ADR-020** — `inset.control` maps `core.spacing.{1|2|4}`, the deliberately tight tune — while `spacing.md`'s summary table claimed `{3|4|6}` under a header saying it reflects the base theme: a doc↔code contradiction, fixed the same day (table + example block). (2) **ADR-020's own justifying sentence is what this entry actually measures broken:** it reads _"the residual fluid drift at `core.spacing.1` is ±2px and **never binds**, since `hit` drives height"_ — false at the top of the unit's range, where content + inset (34px) exceeds the 32px floor, which is exactly the 32 → 34 ramp measured here. Below ~900px the floor drives the height as ADR-020 says; above it the fluid inset does. (3) The docs already sanction both exits: `spacing.md` makes fluid-from-one-engine the family's identity **and** mandates bounded `clamp` contracts where the resolved outcome is a guarantee (`gutter.*`, `separation.interactive.min` fail validation unbounded; `inset.action.block` is bounded 8–9px _so a CTA resolves ~40px_ — the precedent sentence). So the decision is a **classification**, not new doctrine: either the control inset is outcome-bearing (bound it, making ADR-019/020's ruling true by mechanism) or the ruling is narrowed in writing to the floor. Not an S2 question — S2 has no fluid engine at all; this is our own ADR's premise against our own measurement.
- **✅ Fixed 2026-07-29 (owner picked the classification: outcome-bearing) — fsl-theme ADR-022.** `inset.control.{sm|md|lg}` is now a **fixed-px contract** (`6px`/`12px`/`24px` — the engine's own desktop values, so nothing changes at ≥1200px), validated by spacing Error #17 (injection-verified) and registered in model.md §8 (a constant cannot be a `TokenRef`; every `core.spacing` step is fluid by design). A bounded band was **rejected with the arithmetic**: the engine's unit tops out only at cqi ≥ 1200, so any band containing its natural range preserves exactly the 32.5px-at-900px drift this entry measured — the band shape works for `inset.action.block` because that token's guarantee is "desktop exact"; this one's is "stable across the range". Measured after, in Chromium at 390/900/1280/1920 **in the Storybook story's 1200px canvas**: the field reads **32 / 34 / 34 / 34** there — the inset's mid-range drift is gone (900px read 32.5 before). The row height is container-dependent and this figure is not universal: control type stayed fluid by the same ruling, so the Studio's narrower form column reads 32 / 32.67 / 34 / 34 at the same viewports (re-measured 2026-07-30 under ADR-023). What is fixed at every width is the **inset** (6/12/24px), which is the guarantee, and the one residual step at 390 is not the inset's: the fluid **type** (kept fluid by the same ruling) drops to 14px there and the content meets the 32px `hit` floor, which is ADR-020's floor doing exactly its job. The `surface ≥ control` validation now compares resolved px at the engine floor because the two sides have different shapes. F-021's type half was ruled the **other way** in the same ADR — fluid type is the system's identity; S2 is reference, not authority. The registration pass also caught `inset.action.block` shipping unregistered in the §8 inventory, against that section's own rule.

### F-036 — the theme's contrast suite pairs text only against its own role's background, so a cross-role pairing is unguarded

- **Date:** 2026-07-29 · **Surface:** `@ttoss/fsl-theme` `tests/unit/tests/theme/families/colors.test.ts` (`extractTextBackgroundPairs`) · **Severity:** gap · **Status:** ✅ fixed (2026-07-31 — P3 Round 0, ADR-024)
- Found while shipping F-032's fix. The `validationMessage` now reads `input.negative.text.default`, and that ink lands on **whatever surface the form sits on** — not on `input.negative.background.default`. `extractTextBackgroundPairs` walks `{ux}.{role}` and pairs `text.*` with the `background.*` of the **same role**, so what the suite verifies for this token is `red.700` on `red.100` — the filled negative input, a surface no field in the family renders.
- **So the pairing that actually ships is verified by hand and by nothing else.** Measured 6.47:1 light and 7.97:1 dark at 1280px, both clearing AA; the point is that neither number is defended. Darkening `input.negative.text.default` to serve its red.100 fill better would keep the suite green while degrading every validation message in the product.
- **Not specific to this token.** Any part that reads one role's ink and sits on another role's surface has the same hole, and the family does that deliberately — §3.2 of `CONTRACT.md` now states the validation message as the intended case. The structural fix is a pairing inventory keyed by _where the part renders_ rather than by which role owns the token, which is the same shape F-024 needs for "quiet ink vs the stratum behind it".
- **Relation to F-027:** F-027 is that the dark bundle's border inventory is unaudited. This is narrower and orthogonal — the pairing does not exist in either bundle's inventory, light included.
- **Backlog:** decide whether the theme suite grows a cross-role pairing inventory (explicit `{ink, surface}` list, failing on any addition or removal, mirroring the border inventory's "no silent drift" shape) or whether `fsl-ui` owns the assertion for the pairings it composes. Leaning theme-side: it is the package that owns both values, and `fsl-ui` cannot see resolved colours in jsdom at all.
- **Resolved 2026-07-31 (P3 Round 0, ADR-024) theme-side, as the lean said**, with one correction the measurement forced: **the surface is not a token, it is a set of strata.** This entry recorded 7.97:1 for dark, and the page resolves 9.53 — the gap is the whole finding. 7.97 is the ink against `elevation.tonal.raised`, i.e. the form was on a card; per `colors.md` › Stacking informational surfaces the page and every contained surface share one background token and differ by the tonal lift, a composite no colour token names. So an ink that "renders on the page" renders on any of four values, and pairing against the page alone verifies the easiest of them. The inventory pairs against all of them.
- **The hole was real and the guard proves it, rather than the entry asserting it.** Retuning the dark ink to serve its own role's background is the scenario this entry described: it clears the same-role pairing at 4.81 and fails the new one at 4.02 on `raised` and 2.89 on `overlay`/`blocking` — measured by injecting exactly that change.
- **One entry, not a mechanism.** The validation message is the case `CONTRACT.md` §3.2 declares; F-024's "quiet ink vs the stratum behind it" did **not** join it, because the owner's ruling there keeps `muted` painting, so its ink stays audited against its own background. The list grows when a part composes this way, not before.

### F-037 — `FormSubmit`'s pending contract existed only in its JSDoc

- **Date:** 2026-07-29 · **Surface:** `@ttoss/fsl-ui` `composites/Form/Form.tsx` (`FormSubmit`) · **Severity:** defect · **Status:** ✅ resolved 2026-07-29 (forms item I)
- Found by the first consumer: the Environments create flow submits against a fictional backend and asserted `data-pending` on the disarmed button — null. `FormSubmit` hand-wrote `data-pending`/`data-composition` attributes and converted `isPending` into `isDisabled`; React Aria's Button computes `data-pending` from its **own** `isPending` prop after the passthrough spread (clobbering the hand-written one with `undefined`), and fsl-ui's Button re-emits `data-composition` from its `composition` prop the same way. Both JSDoc promises had stood for two items with nothing failing — a comment has no oracle.
- **The fix subtracted code:** React Aria's Button already ships the whole pending mechanism, strictly better than the hand-rolled shape — `aria-disabled` instead of `disabled` (the button stays focusable; a control vanishing from under the keyboard user mid-submit is the classic pending defect), press blocked, the `type` rides as `button` while pending so implicit submission stops routing through it, the state change announced to AT, and `data-pending` emitted natively. `FormSubmit` now forwards `isPending` and `composition` as Button props and writes no attributes by hand.
- **What stays open by design, measured while writing the guard:** the form's own implicit submission (Enter in a lone text input) fires on the `<form>` element, out of any button's reach — in a multi-input form the type swap happens to suppress it (no submit button, no implicit submission), in a one-input form it does not. A host that owns the submission lifecycle re-entry-guards its handler; the Environments flow does, with a test dispatching a raw submit during the pending window.
- **Guards:** `formValidationBehaviour.test.tsx` — pending marks/names/stays-focusable, press blocked; plus the first `validationErrors` guard (server error routed by `name`, withdrawn on commit — read in `useFormValidationState`: `commitValidation` is what clears server errors, so the withdrawal happens on blur, not per keystroke).

### F-038 — a fixed semantic outcome was written as a literal instead of a core step

- **Date:** 2026-07-30 · **Surface:** `@ttoss/fsl-theme` `src/baseTheme.ts` › `semantic.spacing.inset.control` · **Severity:** defect (token-model violation) · **Status:** ✅ resolved 2026-07-30 (ADR-023)
- Found by the owner in review of #1181, against the spacing family's own contract table: the `inset.control.*` rows read `fixed px — outcome-bearing (ADR-022)` with a default mapping of `6px`, while every sibling row maps to `core.spacing.N`. The question — do core tokens supply semantic values, and is the value fixed or responsive? — is the one the table's own shape answers everywhere else.
- **The nature of it.** ADR-022's ruling was right (a control's box is inset + type over the `hit` floor, so the inset must not be container-fluid) and its numbers were right. The _mechanism_ was not: the constant went into the semantic layer with a §8 `RawValue` entry whose necessity argument read "a constant cannot be a `TokenRef` because every `core.spacing` step is fluid by design". That sentence describes a **missing core step**, and §8's test asks whether the value "cannot be expressed as a single `{token.path}` reference" — which a constant always can be, the moment core holds it. Core is the layer whose job is holding values (§1). So the exception was granted circularly and the result was a semantic token that referenced nothing (§2) — the one shape the architecture has no room for. Exactly the CLAUDE.md trap in a new place: _"the theme has no token for this"_ was a gap to fill, not a licence to inline.
- **It was the only instance, and the discriminator generalizes.** A scan of the whole semantic layer found three non-ref values that are not compositions, and all three were `inset.control.{sm,md,lg}`. Every other `RawValue` (`gutter.*`, `separation.interactive.min`, `inset.action.block`, `measure.reading`, `overlay.scrim`) is a `clamp()`/`rgba()`/`ch` **composition** — genuinely inexpressible as one ref, which is what the exception exists for. The rule now written into model.md §8 and enforced with no exception list: a **bare constant** is never a lawful semantic value; a composition may be.
- **What the fix bought beyond compliance.** The emitted CSS chain is whole again: `--tt-spacing-inset-control-sm` now resolves through `var(--tt-core-spacing-fixed-1)`, so a theme that retunes core spacing moves control insets — with the literal inlined it could not, silently. And the semantic-ref guard's escape hatch turned out to be **dead** once the real offender moved (its `sizing.hit`/`sizing.measure` entries had already become refs or compositions), so the guard runs with no exceptions, injection-verified against the exact shape that shipped.
- **Second finding, from re-measuring:** ADR-022 asserts the post-fix row as "32 / 34 / 34 / 34" with no container named. The row is inset + **fluid type**, so that figure is container-specific — the Studio's form column reads 32 / 32.67 / 34 / 34 at the same viewports. Corrected in four files with the container stated; the guarantee that _is_ universal is the inset resolving 6/12/24px at every width.

### F-039 — in dark, an engaged or marked edge takes a brand step chosen for the light page

- **Date:** 2026-07-31 · **Surface:** `@ttoss/fsl-theme` `darkAlternate` › `*.border.{focused,current,hover,active,selected}` on brand/accent · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-02 — **(b) fixed, (a) was not a defect**
- Exposed by the F-027 audit, which is the point of building it: these were sub-threshold in dark for as long as the dark alternate has existed, and the guard that would have said so ran only on light. Two shapes of the same question, both measured at both ends.
- **(a) The focused edge keeps the base's brand step.** `input`, `informational` and `feedback` write `border.focused` as brand.500 in the alternate, which lands on the mid-grey engaged fills at 2.26:1 — while `navigation` lifts the same token to brand.400, and the focus ring itself is lifted further to brand.300 (9.04:1 on the canvas). So one family decided the dark canvas needs a lighter step and the others did not.
- **(b) The accent cascade inverts.** `informational.accent` goes 6.16 at rest → 2.46 on hover/active, because the fill lightens a step while the border darkens one; the base moves both the other way (3.06 → 4.18). `navigation.accent.{current,selected}` sit at the same 2.46.
- **Why this was not fixed with the four remaps beside it.** The audit's line: applying a decision the alternate had **already made elsewhere** is a fix (that is what the negative border subtree, the two engaged field edges and `navigation.muted.current` were — omissions, provable from the alternate's own siblings). Choosing a new ramp step is the owner's. Both shapes here need a step chosen, and (a) additionally needs a ruling this system has never written down: **does `border.focused` owe 3:1 when a compliant ring is already drawn?** ADR-011 keeps `focus.ring` a separate family, and the light inventory has always accepted a sub-threshold focused border on the reasoning that the ring carries focus — but it accepted it for filled surfaces, not for fields on a page.
- **The (a) answer constrains its own fix:** lifting hover and default to the same step trips the distinguishability guard (`hover` must differ from `default`), so it is a two-step choice on the brand ramp, not a substitution.
- **Backlog:** owner pass in the next P3 round — rule (a) first, since it decides whether nine contexts are a defect or a documented pattern, then pick the accent cascade's dark steps. The guard now fails on any movement in either direction, so whatever is chosen stays chosen.
- **Answered 2026-08-02 by reading the docs first, which is what this entry should have done before escalating.** The ruling (a) asked for is **already written**: `colors.md` › Cross-cutting tokens carries a "Focus color — which token to pick" table. What it says, however, is the **opposite of the code** — it routes `Action`/`Input`/`Navigation`/`Feedback` to `{ux}.{role}.border.focused` and reserves `semantic.focus.ring.color` for an Informational surface with no obvious `{ux}`, while `tokens/focusRing.ts` states the ring comes "always from the cross-cutting `vars.focus.ring.*` tokens … **never from per-entity color subtrees**".
- **Measured rather than argued:** of the files that resolve a focus state, the ones that never call `focusRingOutline` all reach it through `ActionTrigger/anatomy` or `Field/anatomy`, which do. **No component uses `border.focused` as its only focus signal** — so the code draws both layers and the table's either/or describes nothing that exists.
- **`model.md` §11 settles which artefact yields:** family docs are tier 3 (consumer guidance), below `Types.ts`; `Types.ts` ships both tokens, so enforcement does not arbitrate which one _indicates_, and the Lexicon says nothing about focus. The divergence is therefore a defect in the family doc. It is also the substantively right answer: `outline` cannot shift layout where `border` can, and on a filled control **no** edge colour clears both the fill and the page — in dark `action.primary` is a white pill — which is exactly why the ring is floated off the edge and why one system-wide colour suffices.
- **So (a) is not a defect and the nine contexts stay as inventory.** What was owed was a doc fix: the section is now "the ring indicates, the border tints", and Required Pairing #3 names the ring as the token that owes it.
- **The real gap it exposed, now closed:** pairing #3 had never been verified for the actual indicator. The ring is floated, so the background it lands on is the stratum — a cross-role pairing, and now the second entry in that inventory alongside the validation message.
- **(b) fixed 2026-08-02.** Pairing #2 is unconditional and the accent's engaged edge failed it with no exemption available (not focus, not disabled, not mirrored). Engaging lifts the fill a step while the base's edge moved _down_ the ramp — the inversion. `informational.accent.border.{hover,active}` and `navigation.accent.border.{current,selected}` take brand.200: one step clear of the resting brand.300 they must also differ from (Warning #1), which brand.300 itself would have violated.

### F-040 — our two chip names are inverted relative to both reference systems

- **Date:** 2026-08-02 · **Surface:** `@ttoss/fsl-ui` `Badge` / `Chip` naming · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-02 (owner decision — renamed)
- Found while resolving F-010, by the collision that blocked the obvious name. In **Spectrum**: `Badge` is color-categorized metadata, `StatusLight` is the semantic status indicator, `TagGroup` manages a user-editable set. In **Chakra 3**: `Badge` is a plain descriptive span, `Status` is root + indicator, `Tag` is the compound with a close trigger. Both call the **descriptive** chip `Badge` and give the **status** one a different name.
- Ours are the other way round: `Badge` is Feedback (the valence reporter — their `StatusLight`/`Status`), and `Tag` is the Selection item inside a `TagGroup`. So the two words a reader already knows both point at the wrong member, and the third member had to take a third word (`Chip`).
- **Why this matters more here than in a human-only system.** The package is AI-first and its first-pass correctness is the thesis. An agent asked for "a role chip" reaches for `Badge` or `Tag` on priors from every other library: `Badge` renders a valence it never asked for, and `Tag` throws for want of a `TagGroup`. `llms.txt` now carries the disambiguation table and an explicit naming warning, which is mitigation, not a fix — the fix is the name.
- **Not renamed now, deliberately.** Both names are published, and a rename cascades through the catalog, the Studio, the stories and the manifests for a benefit that is real but not urgent. It is also exactly the kind of change that belongs to a version boundary rather than to a slice.
- **Backlog:** decide at the v1.0 break whether to align with the references — `Badge` becomes the descriptive chip, the status member takes `StatusLight` or `Status`, and `Chip` folds into `Badge`. Mechanical if taken then; awkward forever if not.
- **Owner took it immediately rather than at the boundary (2026-08-02), and taking it now was the cheaper half of the trade.** `Chip` folded into **`Badge`** (Structure, descriptive) and the status member became **`StatusLight`** (Feedback, valence) — Spectrum's exact split, and the one Chakra makes under different words. The names now mean what an agent's priors expect, which is the whole point in a package whose thesis is first-pass correctness.
- **Why immediately was right:** the window this entry worried about was "mechanical now, awkward forever". `Chip` had existed for hours and had three consumers; the rename touched two source files, two suites, two stories and six Studio sites, and every one of them was mechanical. Waiting would have added every consumer written in between to the same migration.
- **The scopes moved with the names** (`chip` → `badge`, `badge` → `status-light`), because a published `data-scope` that disagrees with its component's name is the ambiguity F-026 and F-030 were filed for, one level up.
- **`CHIP_BOX` kept its name deliberately.** It is no longer any component's name, which is exactly right for the shared source: it names the _physical object_ two components render, and the entity decides only its colours.

### F-041 — no in-system way to pick a responsive variant

- **Date:** 2026-08-02 · **Surface:** `@ttoss/fsl-ui` (+ `@ttoss/fsl-theme` breakpoints) · **Severity:** gap · **Status:** ⛔ closed 2026-08-02 — answered by the docs: application layer
- Split out of F-023 rather than absorbed into it. `AppShell` can now express both sidebar shapes and the reference systems agree that choosing between them is the app's call — but they also both ship the helper that makes the choice: MUI has `useMediaQuery`, Chakra has `useBreakpointValue`. We ship neither, so an app hand-rolls `matchMedia` and re-derives the breakpoint values it cannot read from the theme in a media query.
- **The constraint is real and documented:** `families/breakpoints.md` states that the emitted custom properties "are for JS/tooling inspection only; they cannot be used in `@media` queries (CSS spec restriction on custom properties)". So a CSS-only answer does not exist, and _inspection_ — reading the resolved value in JS — is the sanctioned use the docs already name.
- **Consequence today:** any consumer that wants the narrow shape writes viewport logic by hand, which is the class of workaround this log exists to prevent, and it will be written slightly differently in every app.
- **Backlog:** a hook that resolves a named breakpoint against the current viewport, reading the value off the emitted custom property rather than duplicating the scale. Package boundary is the open question — `fsl-theme` owns the values and already ships React (`react.tsx`), while `fsl-ui` is where the consumer lives.
- **Closed 2026-08-02: the boundary was never open — `breakpoints.md` had already placed it, and the entry was filed without reading it.** The family doc states that breakpoints are "adaptation infrastructure, not visual design tokens", that they define no semantic layer, that "**applications may adjust or replace them** based on real layout needs", and — naming this exact case — that "any local aliases (e.g. **`navCollapse`**, `shellWide`) **stay in the application layer**".
- **So neither package should ship the hook.** `fsl-theme` publishing a viewport hook would give a semantic-free infrastructure family a runtime the doc says applications own; `fsl-ui` publishing one would make components depend on viewport thresholds, which the same page forbids in its own words: "**breakpoints define _when_ layout changes, not _how_ components behave**".
- **The friction was real and it was pointing at the wrong layer.** What the doc leaves to the app is a handful of lines it wants to own anyway, over a scale it is explicitly allowed to replace; the emitted custom properties are readable from JS for exactly this ("for JS/tooling inspection only").
- **Same page also settles when breakpoints are the wrong tool at all:** "a single component needs to adapt → **container queries**". A component adapting to its own width never reaches for this.

### F-042 — the Tabs-as-navigation workaround cannot move into a Drawer, so the shell's narrow shape has no consumer

- **Date:** 2026-08-02 · **Surface:** `@ttoss/fsl-studio` `AppFrame` (`Tabs` as primary nav) × `@ttoss/fsl-ui` `AppShell sidebarVariant="temporary"` · **Severity:** gap · **Status:** ✅ resolved 2026-08-03 (closed with F-002)
- Found while wiring Meridian to the narrow shell shipped for F-023, which is the first time the two workarounds met. The shell collapses correctly and the drawer opens with its accessible name — and the `tablist` inside it renders **empty**: `<div role="tablist" aria-label="Workspace" />` with no `tab` children. React Aria's collection does not populate a `TabList` portaled into a `Modal`, so the navigation exists as a labelled shell with nothing in it.
- **This is two recorded workarounds colliding, not a defect in either component.** `AppShell`'s temporary variant is proven in isolation (its own suite, and the Storybook story). Meridian's sidebar is a `TabList` only because `Link` cannot mark `aria-current` (F-002), and the whole frame lives inside one `Tabs` because a `TabList` without panels emits invalid ARIA (F-017). A `TabList` is a collection owner; a drawer is a portal. The two cannot be the same element.
- **Not worked around, and the app-layer threshold was reverted with it.** A `useNavCollapse` hook and the `sidebarVariant` wiring were written and then removed rather than shipped half-working: leaving them in would hide the navigation on a phone, which is worse than the horizontal scroll F-023 measured. The Studio keeps the permanent shell until its navigation stops being tabs.
- **What it changes about F-002.** That entry was parked on a token-lifecycle ruling (no token leaves while the package stabilises), and the parking is unaffected — but its cost is now larger than "a sidebar cannot mark the active page". It also blocks the narrow shell for the only app that needs it. When `Link` gains `current`, the Studio's nav becomes links, the drawer holds them without a collection owner, and this closes with it.
- **Backlog:** re-attempt once F-002 lands. Nothing to build here in the meantime; the capability it consumes already exists.
- **Resolved 2026-08-03, one day later, by the fix this entry predicted.** `Link isCurrent` landed, the Studio's sidebar became links, and the re-attempt worked on the first try: a list of links has no collection owner, so nothing needs the portal to register anything. Meridian now runs `sidebarVariant="temporary"` below the `md` threshold, with the app-layer `useNavCollapse` alias the breakpoints doc names.
- **The lesson is about the shape of the diagnosis, not the fix.** Neither component was defective and neither needed changing; the failure lived in the interaction of two workarounds, and it was only visible when both were exercised at once. It took building the second capability to find that the first one's cost had grown.

### F-043 — a background state with no matching text state renders an unaudited pair, and one of them fails at 1.45:1

- **Date:** 2026-08-03 · **Surface:** `@ttoss/fsl-theme` `semantic.colors.action.secondary` (dark alternate) × `fsl-ui` `resolveInteractiveStyle`'s `?? text.default` fallback · **Severity:** defect (WCAG 1.4.3) · **Status:** ✅ resolved 2026-08-04 (fsl-theme ADR-026 — instrument first, then the values)
- Found in the browser while verifying F-029's Storybook stories, not by any test. An open `MenuTrigger` with a `secondary` trigger renders, in dark, `rgb(248,248,248)` on `rgb(208,208,208)` — **1.45:1**, a label that is effectively invisible. React Aria marks a trigger `isPressed` for as long as its overlay is open, so this is not a flicker during a click: it is the trigger's appearance for the whole time the menu is on screen.
- **Mechanism, and it is a guard hole rather than a careless value.** `extractTextBackgroundPairs` pairs `text.<state>` with `background.<state>` of the same role and **skips when either side is absent** — the fsl-ui CLAUDE.md already names this asymmetry as trap #2, in the other direction. Here the _text_ side is absent: the dark alternate lifts `action.secondary.background.active` to `neutral.300` and writes no `text.active`, so the suite has no pair to check. The component renders one anyway, because `resolveInteractiveStyle` is strict and every Action call site appends `?? colors?.text?.default`. **A state that exists on one dimension and not the other is therefore invisible to the suite and visible to the user.**
- **Why the theme looks careless and is not.** It _did_ write the inversion — `action.secondary.text.pressed` is `neutral.900` on a `neutral.300` fill, exactly right. But `pressed` and `active` are two different consumers: `STATE_PRIORITY` maps `isPressed → active` for every component that goes through the cascade, while `ToggleButton` reads `*.pressed` directly for its selected state (its own documented divergence). The ink was authored for the toggle and the momentary press got the fill without it.
- **Bounded, measured across both bundles and both modes** (probe run 2026-08-03, deleted after use — the instrument below is what should replace it). Light is unaffected: `background.active` is `neutral.400` there and the resting ink clears it. The dark alternate produces exactly one text pair below AA: `action.secondary.active`. Six further rows surfaced on `input.*.indeterminate`, and they are **not** claimed as defects here — the indeterminate mark is a glyph inked from `SELECTION_CONTROL`, not text, so whether `text.indeterminate` is ever a rendered pair needs checking before any of them counts.
- **Two candidate fixes, both one line, and the choice is a design call:** add `action.secondary.text.active: neutral.900` to the dark alternate (mirrors what the same role already does at `pressed`, keeps the engaged lift), or drop the dark `background.active` lift so the ink never needs to move. The first matches the light mode's own mechanism.
- **The instrument is the more valuable half.** The pairing suite should assert the pair a component would actually render — `text.<state> ?? text.default` against `background.<state>` — rather than skipping when the ink side is missing. That closes the class, not the instance, and it is the same shape as F-027's fix for the border pairing.
- **Backlog:** ship the instrument first, then take whatever it lists — including the `input.*.indeterminate` rows, once someone establishes whether they are rendered pairs at all. Not folded into F-029: that change touched no theme value, and a dark-alternate colour retune belongs in its own commit with its own guard.

- **Resolved 2026-08-04, instrument first, exactly as queued — and the docs already held both halves of the design.** Pairing #1's own words — _"corresponding is where the part renders, not who owns the token"_ — are the instrument's definition: the extractor now walks every declared `background.<state>` and pairs the ink the component contract actually renders (`text.<state>`, else the documented fallback; `indeterminate` resolves through `checked` first, the selection mark's chain). The registry's definition of the `text` dimension ("readable foreground, labels, and **text-like icons**") answered the indeterminate question this entry left open: the mark is a rendered pair and owes AA Normal, and a selected row puts running text on `background.checked` besides. **192 previously unaudited pairs entered the suite; seven failed, in three classes:** the valence/muted indeterminate inks (the chain drags `checked`'s `neutral.0` — the filled box's label — onto the light indeterminate fill: 1.4–1.9:1, both modes, so the fix is a base declaration both modes inherit); the headline `action.secondary.text.active` (the alternate had inverted `pressed`'s ink but not `active`'s); and `informational.{valence}.background.selected` (the alternate remapped the valence text to light inks while the selected fill inherited the base's light tint — 1.0:1).
- **The first fix failed in the browser and that failure is the second guard.** `text.active` declared only in the alternate turned every suite green and changed nothing on screen — `vars` mirrors the **base** shape, so an alt-only leaf emits a custom property no component can reference. `model.md` § Modes already ruled it ("semantic token names do not change"); a structural guard in `global.test.ts` now fails any bundle whose alternate declares a path the base does not, and the scan that motivated it found exactly one violation in the whole theme: that first fix. The doctrine held everywhere except the change made that morning.
- **Verified:** two seeded mutations each fail the suite in every affected bundle and mode; the structural guard catches the alt-only shape; Chromium shows the open menu's trigger label at `rgb(22,22,22)` on `rgb(208,208,208)` in dark (was `rgb(248,248,248)`), with light byte-identical. fsl-theme 1214, fsl-ui 2707, Studio 107.
- **Lesson:** a guard that iterates only what is declared audits the theme's _bookkeeping_; the contract is what components _render_. When a fallback is part of the component contract, the fallback is part of the pairing — and a green suite plus an unchanged screen is itself a finding worth chasing to the root.

### F-044 — an anchored overlay has no audited separation from the page it floats over

- **Date:** 2026-08-04 · **Surface:** `@ttoss/fsl-theme` `semantic.colors.informational.primary.border.default` × every Overlay in `fsl-ui` · **Severity:** defect (WCAG 1.4.11; `colors.md` states the requirement this misses) · **Status:** ✅ resolved same day (fsl-theme ADR-027 / fsl-ui ADR-031)
- Found in the P3 Overlay round, first component measured. Every anchored surface the package ships — the `Menu` popover, `Popover`, `Tooltip`, the `Dialog` panel, the `Drawer` panel — paints `informational.primary.background.default`, which is **byte-identical to the page** (1.00:1, by design: `colors.md` § Stacking has the page and every contained surface resolve from one token). Its only edge is `informational.primary.border.default`. **Measured against the page: 1.31:1 in light, 1.67:1 in dark.** So the whole figure/ground separation of a floating surface rests on a `box-shadow`.
- **The doc already assigns that edge the duty it does not meet.** `colors.md` § Stacking, separator #2: _"`border.outline.surface` is the secondary separator. A 1px outline at **≥ 3:1 contrast against the adjacent background** guarantees a perceptual edge **even when shadow is suppressed (high-contrast preferences, print)**."_ That sentence names the exact failure case: under `forced-colors`, a user stylesheet, or print, the shadow is gone and a menu becomes an unbounded rectangle of page-coloured text. This is not an interpretation of the doc — it is the doc's own number.
- **Why the guard cannot see it, and it is F-043's asymmetry one dimension over.** `extractBorderBackgroundPairs` pairs an edge against **its own role's** background, so it evaluates `informational.primary.border` against `informational.primary.background` — the same 1.31:1 — and `informational.primary.default` is a listed member of the accepted **soft** inventory. That acceptance is correct for what it audits (an edge on its own fill carries no separation duty) and blind to the pair that matters: the overlay's edge against **the stratum beneath it**, which is a different background and, per the doc, a ≥3:1 duty. Same shape as the validation message (F-036) and the focus ring (F-039): the pair that carries the signal is cross-stratum, and only a cross-stratum entry can audit it.
- **Not the shadow's job to fix.** Shadows are not colour tokens, are never audited, and are the thing the doc's clause is a fallback _for_. `elevation.surface.overlay` is doing real work and stays; what is missing is the edge that survives without it.
- **Two candidate fixes, and the choice is a design call.** (a) Lift `informational.primary.border.default` to a step that clears 3:1 against the page (light: `neutral.400` ≈ 3.0:1 against white; dark: `neutral.500` ≈ 3.6:1 against `neutral.900`) — one token, reaches every surface at once, and makes every card edge more present, which is a visible identity change. (b) Give the _overlay strata_ their own edge (a `border.outline.surface` counterpart the Overlay entity reads while `Structure`/`Collection` keep today's hairline) — narrower blast radius, but it is a border-registry addition and needs the entity mapping to say which stratum owes what. **The instrument comes first either way:** a cross-stratum border pairing, so whichever value ships is audited against the page rather than against itself.
- **Resolved the same day, and neither (a) nor (b) is what shipped — the root cause named a third shape.** The blast radius that made (a) unattractive was the giveaway: `informational.{role}.border` is one token serving _three_ duties (a divider, an embedded card's decoration, an occluding surface's boundary), and the first two want the hairline the soft inventory already blesses. So the boundary became **`semantic.overlay.outline`**, cross-cutting per model.md §6 and a sibling of `scrim` — which is the right family by its own logic: `scrim` is what an occluding surface puts _behind_ itself, this is what it puts _around_ itself. The §6 gate passes on the same test F-029/ADR-025 established: occlusion is neither role nor state, and it **crosses ux** (a Menu is `informational`, a Toast is `feedback`, both cover content), so the grammar cannot ask for it. No registry change was needed — `semantic.overlay.` already maps to `--tt-overlay-` with DTCG `color`.
- **The instrument came first and it retired the fancier design.** The cross-stratum pairing ("occluding boundary") measures the candidate against every stratum an overlay can land on — page, both tonal lifts, a row fill, a voiced surface. Result: **one value per mode clears 3:1 against all of them** (light `neutral.500` 5.02–4.41, dark `neutral.300` 11.73–3.26). The per-stratum `elevation.edge.*` companion that option (b) implied was therefore membership-guessing for granularity nothing uses, and was dropped. Mutating the light value to `neutral.300` fails the pairing at 1.54 in five places, so the guard is live.
- **`evaluation` costs nothing by stepping aside.** Measured across both modes before the change, `informational.{primary,secondary,muted}.border.default` all resolved the _same_ value — so the prop never varied an overlay's edge. The boundary is infrastructure, the same argument that gives the focus ring one system colour.
- **Verified in Chromium, both modes:** the menu's edge reads `rgb(111,111,111)` in light and `rgb(208,208,208)` in dark, and it reads as an ordinary bounded menu rather than a heavy outline. `Surface`/`Box` are unchanged, asserted from the other side so "put the boundary everywhere" cannot pass.

### F-045 — anchored surfaces read the page-surface inset scale, and it is fluid over fixed content

- **Date:** 2026-08-04 · **Surface:** `@ttoss/fsl-theme` `semantic.spacing.inset.surface.{sm,md}` × `Menu`/`Popover`/`Tooltip`/`ListBox`/`GridList` · **Severity:** gap · **Status:** ✅ resolved same day (fsl-theme ADR-027 / fsl-ui ADR-031)
- Found in the same round. `inset.surface` has three steps whose **tightest is 16–24px** (fluid, `core.spacing.4`), and every anchored overlay reads it. Measured at 1280px: the `Menu`'s content padding is **24px around rows that are 32px tall** — the surface is 192×146 for three rows, so **34% of its height is padding** and the rows visibly float away from the edge; `Popover` takes `md` (**36px**); `Tooltip` takes **24px block / 36px inline for a single line of text** (236×70 for one 20px line). Reference, for the same parts: `popover-padding` **8px** uniform, `component-top-to-text-100` **6px** / `component-edge-to-text-100` **12px**. Ours is 3–4.5×.
- **A second defect underneath the magnitude, and this one is a ruled class.** The scale is **fluid** while the rows it frames are **fixed**: measured 390 → 1920px, the menu's padding moves **16px → 24px (+50%)** while every row stays exactly 32.0px. ADR-022 already ruled this shape for controls — _"a control's box is its inset + type over the `hit` floor, so the inset is outcome-bearing; a fluid inset makes the box container-fluid"_ — and a frame whose relationship to fixed-height content is the whole visual outcome is the same argument one context over.
- **The class is narrower than the reader list suggests, which is what makes it fixable.** Same defect: `Menu` content, `Popover`, `Tooltip`, `ListBox` root, `GridList` root — containers whose padding frames their own fixed-height rows or a single line. **Not** this class, verified individually: `Code`'s block padding and `Tabs`'s panel block padding (page-level regions, 24px is right), `Box`/`Surface` (the step is caller-chosen — that is their whole purpose), and `Dialog` (36px on a 498px panel; S2's standard-dialog medium max is 480px, so ours is aligned).
- **The vocabulary is already legal, which is F-016's shape.** `spacing.md` lists `xs` in the step vocabulary (`step: xs, sm, md, lg, xl, min`) and `gap.stack.xs` already ships — so an `inset.surface.xs` for the anchored/row-framing class needs **no registry growth**, and ADR-022/023 already fix its mechanism: an outcome-bearing inset is a **fixed** core step, and `core.spacing.fixed.1` (6px) exists. Retuning `sm` instead was considered and rejected: `Box`/`Surface` publish `sm` as a caller-facing step, so retuning it changes an API surface to fix a different class's problem.
- **Resolved the same day, exactly as the backlog specified.** `inset.surface.xs → core.spacing.fixed.1`, consumed by the five parts. The measured outcome: the menu's gutter 24px → 6px and the surface **146px → 110px** for the same three rows, so chrome fell from 34% of its height to 13% — the reference's proportion. The step joins `inset.control` in the **fixed-px** contract rather than the fluid aliasing one, which is where ADR-022's argument puts it, and the guard was renamed to say what the two groups have in common instead of naming one of them.
- **The Tooltip's inline half went uniform rather than borrowing `inset.control.md`.** Taking the control step would have been an entity-row violation — Overlay's §1 row is `inset.surface` — and it was unnecessary: the reference's own anchored padding is a _single_ value (`popover-padding`), not a pair, so uniform `xs` is the reference's shape as well as the lawful one.
- **The class held under implementation.** `Code`, `Tabs`'s panel, `Box`/`Surface` and `Dialog` were each verified individually to be right before the change and are untouched by it — the discriminant is whether the padding gutters children that carry their own inset, not whether the surface floats.

### F-046 — `Dialog` has no minimum width

- **Date:** 2026-08-04 · **Surface:** `@ttoss/fsl-ui` `Dialog` · **Severity:** paper-cut · **Status:** ✅ resolved same day (fsl-ui ADR-031)
- `Dialog` declares `--fsl-dialog-max-width` (`min(500px, 90vw)`) and `--fsl-dialog-max-height`, and **no minimum**. The reference sets both: `standard-dialog-minimum-width` **288px**, `alert-dialog-minimum-width` **288px**. A confirm dialog whose body is one short sentence therefore collapses to its content width, and a two-button action row becomes the widest thing in it — the `ConfirmationDialog` shape exactly.
- **Measured:** the `Default` story renders 498px because its body wraps to two lines; nothing holds the floor when it does not.
- **Resolved the same day as specified.** `--fsl-dialog-min-width` ships beside the two knobs it pairs with, defaulting to `min(288px, 90vw)` — the reference's minimum for both its standard and its alert dialog, clamped so it still fits a 320px viewport.

### F-047 — `Tooltip` is sized for a paragraph, not a hint

- **Date:** 2026-08-04 · **Surface:** `@ttoss/fsl-ui` `Tooltip` · **Severity:** paper-cut · **Status:** ✅ resolved same day (fsl-ui ADR-031)
- `TOOLTIP_MAX_WIDTH_DEFAULT` is `min(280px, 90vw)`. Reference: `tooltip-maximum-width` **160px** desktop / 200px mobile. Ours is 75% wider than the widest reference value, which invites the paragraph a tooltip is not for — and combined with F-045's padding it renders a 236×70px slab for one 20px line.
- **Not merged into F-045** because it is a different mechanism: F-045 is a shared token read by five parts, this is one component's own literal. Fixing F-045 alone leaves a tooltip that is still too wide; fixing this alone leaves one that is still too padded.
- **Resolved the same day, at 200px rather than 160px.** The reference ships 160px desktop / 200px mobile; our label type is a step larger than theirs (F-021 ruled the type scale is the system's own identity), so the same phrase needs the wider of their two numbers to hold the same line count. Measured with F-045's padding fix in the same pass: the tooltip now renders as a bounded hint chip instead of a 236×70 white slab.

### F-048 — an overlay claims the `overlay` stratum's shadow while painting the `flat` stratum's colour

- **Date:** 2026-08-04 · **Surface:** `@ttoss/fsl-ui` `Menu`/`Popover`/`Tooltip`/`Dialog`/`Drawer`/`Toast` vs `Surface` × `@ttoss/fsl-theme` `semantic.elevation.tonal.*` · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-06 (fsl-ui ADR-037) — owner ruling delegated this round
- Split out of F-044's root-cause analysis, which is the only reason it was found: the finding there was "`elevation` is the only family that knows a surface floats", and checking who reads that family completely turned up **exactly one component**. `Surface` reads both halves — `elevation.surface[level]` for the shadow _and_ `elevation.tonal[level]` for the fill companion. Every actual overlay reads only the shadow and takes its fill from `informational.{evaluation}.background.default`, which is the **flat** stratum's colour.
- **Measured, dark, before:** `Surface level="overlay"` painted `#3d3d3d`; a `Dialog` panel painted `#161616`. Two components declaring the same stratum, different colours — the F-025 class inverted (there, two roles that should differ rendered identically). In light the two coincided, because the base theme maps every tonal step to `neutral.0`, which is why this went unseen.
- **Resolved by option (b) from this entry's own backlog: `Surface` takes the overlays' fill rule, `tonal` stays opt-in.** Option (a) — moving the six overlays onto `tonal` — was costed here as raising dark separation from 1.00:1 to only 1.67:1, still under the ≥3:1 the doc requires, so it would not have closed F-044 either; and it would have multiplied `tonal`'s consumer count from one to seven the same round F-057 found `tonal`'s dark values colliding with `Surface`'s own fixed border. Widening a family already in that state was rejected as compounding a known problem rather than fixing one. `backgroundFor` now returns `vars.colors.informational[evaluation].background.default` at every level — the exact expression `Menu`/`Popover`/`Dialog`/`Drawer` already use via `voicedSurface` — and `Surface` itself now calls `voicedSurface` too, so a `muted` (default) `Surface` keeps its own voice and does not publish `--fsl-surface`, matching a `muted` `Menu` and leaving the already-audited quiet-control pairings untouched.
- **What `evaluation` means on a lifted surface, answered:** the fill, at every level — not just the edge. `Surface`'s published behaviour changes on all four levels (`raised`/`overlay`/`blocking` used to ignore `evaluation` for fill entirely; `flat` already read it via the primary fallback, so only its fallback path changed). Cost, stated: a story that showed the four levels as four different tints (`Structure/Surface` → `Levels`) now shows four identical fills differing only by shadow — the story's own doc comment was rewritten to say so rather than leave the stale claim, and a new `Evaluations` story demonstrates the fill actually responding to `evaluation`.
- **Measured, dark, after (Chromium, `structure-surface--evaluations`):** `Surface level="overlay"`/`"blocking"`, `evaluation="primary"` → `#161616`, matching `Dialog`'s `#161616` exactly (same story comparison as the "before" row, `overlay-dialog--default`). `evaluation="muted"` → `#6f6f6f`, matching a `muted` `Menu`'s own fill.
- **Side effect: closes F-057.** See that entry — the border/fill collision F-057 measured was a direct consequence of the fill being `tonal` while the border stayed `informational.{evaluation}.border.default`; reading the same role's `background` for both closes it, verified with numbers there.
- **`elevation.tonal` stays in the theme, unread by any fsl-ui component** — opt-in per this entry's own phrasing, for a future consumer that wants a colourless elevation cue. Not deleted, and its own tests are unchanged.
- Guarded from both sides in `tests/unit/tests/Surface.test.tsx` (fill reads `informational[evaluation].background.default` at every level; never reads `elevation.tonal`, verified to fail on revert) and `tests/unit/tests/surfaceScope.test.tsx` (a `primary` `Surface` publishes; a `muted` one does not, mirroring the overlays).

### F-049 — an anchored surface announces as a dialog wrapped around its menu

- **Date:** 2026-08-05 · **Surface:** `@ttoss/fsl-ui` `Menu` (and every anchored surface) × `react-aria-components` 1.19 · **Severity:** paper-cut · **Status:** ✅ closed on the spot — will not do (fsl-ui ADR-032)
- Found by P3 review round 2 while pinning the APG menu-button contract. The open menu's DOM is `div[role="dialog"]` → `div[role="menu"]` → `div[role="menuitem"]`, and the wrapper's name resolves to the **trigger's own label** via `aria-labelledby`. APG's menu-button pattern has no dialog in it, so a reader entering the surface announces "Open, dialog" before it reaches the menu.
- **Not ours, and not a divergence from the reference.** RAC 1.19 renders `role="dialog"` on any popover it is not told is `isNonModal` (`private/Popover.mjs`: _"Automatically render Popover with role=dialog except when isNonModal is true, or a dialog is already nested inside the popover"_), and it focuses that wrapper on open so the surface is announced at all. The reference system is built on the same primitive and ships the same shape. Passing `isNonModal` to strip the role would also strip the hidden dismiss buttons upstream renders for screen-reader users in the same branch — a worse trade for the same population.
- **What was verified rather than assumed:** the trigger's `aria-controls` resolves to the `role="menu"` element, not to the wrapper, so the pattern's actual pointer is correct. That is now asserted (`overlayBehaviour.test.tsx`), because it holds by construction rather than by intent and a refactor could move it to the wrapper without failing anything.
- **Readmission criterion:** a screen-reader measurement showing the wrapper's borrowed name displaces or delays the menu's own announcement, or upstream making `isNonModal` the menu default — at which point this is a one-prop change, not a design question.

### F-050 — a Feedback fill and the rail behind it resolve to the same colour in dark

- **Date:** 2026-08-05 · **Surface:** `@ttoss/fsl-ui` `ProgressBar`/`Meter` × `@ttoss/fsl-theme` `feedback.muted.border` · **Severity:** blocker (the component renders none of the information it exists to carry) · **Status:** ✅ resolved same day (fsl-ui ADR-033)
- Found by P3 review round 3, measuring the Feedback family against `@adobe/spectrum-tokens`. Both rails read `feedback.muted.border.default` as their **fill**. The dark alternate remaps that token to `neutral.500` — correct for its actual duty, since an edge must lighten to stay visible on a dark canvas — and `feedback.primary.background.default` is **also** `neutral.500` in dark. **Measured 1.00:1**: `<ProgressBar evaluation="primary" value={40} />` painted a uniform grey rail with no visible fill, in both the base and `bruttal` bundles. Verified in Chromium at 900px: before, one flat rail; after, a 40% segment.
- **Only the achromatic evaluation is affected, and that is the point.** The other four fills differ from the old rail by 1.02–1.04:1 in dark yet remain perfectly legible, because they differ in hue and a contrast ratio measures only luminance. `primary` is the one evaluation where luminance is the sole channel — so the ratio understates four cases and is exactly right about the fifth. The rule the fix pins is therefore **identity, not contrast**: how much a fill must differ from its rail is a design judgement the reference itself does not hold uniformly (its own dark accent-on-track measures 2.56:1), but a fill that resolves to the rail's own value renders nothing in any theme.
- **Why no suite saw it.** The border pairing audits an edge against **its own role's** fill; the text pairing audits ink; the role-distinguishability suite (F-025's fix) compares whole `(background, border, text)` triples **within** a role. This pair is one role's background against another role's border, across roles _and_ across dimensions — the third instance of the F-043/F-044 asymmetry class: the inventories are organised by token **kind**, and the defects live in what a component paints next to what.
- **Resolved by reuse, not by growth.** The rail now reads `feedback.muted.background.default`, which is what both the family docs and `baseTheme`'s own `feedback` comment already said it was — _"`muted` stays a tinted neutral surface — the rail/track color for Feedback fills (ProgressBar, Meter)"_. Code had diverged from a ruling written in two places. Dark lands on `neutral.700` (`#3d3d3d`), **four units off the reference's own dark track** (`rgb(57,57,57)`), and the reference confirms the direction: its `track-color` goes `rgb(218,218,218)` → `rgb(57,57,57)`, i.e. a rail **darkens** in dark while a border lightens.
- **What it costs, stated rather than hidden:** in light the rail is now quieter than the reference's — 1.14:1 against the page versus their 1.40:1 — so the bar's total extent reads more faintly. That is the half a dedicated address would recover, and it is F-051.
- **Guarded on both sides.** fsl-ui `rail.test.tsx` asserts the quiet surface is read **and** the quiet border is not; fsl-theme's colour suite asserts no `feedback` role's resting fill resolves to the rail's value, in every mode of every bundle. The theme guard was verified to fail on injection, reproducing the shipped defect verbatim (`feedback.primary.background.default == rail (#6f6f6f)`) in both bundles' dark alternate.

### F-051 — the rail has no address of its own, so three components borrow three different tokens

- **Date:** 2026-08-05 · **Surface:** `@ttoss/fsl-ui` `ProgressBar`/`Meter`/`Slider` × `@ttoss/fsl-theme` `feedback.muted` / `input.primary` · **Severity:** gap · **Status:** ✅ resolved 2026-08-06 (fsl-theme ADR-028, fsl-ui ADR-036) — owner ruling delegated this round
- The root cause under F-050, and it outlived it. **The model had no address for "the unfilled part of a track", so every component that needed one borrowed a token whose meaning was something else:** `ProgressBar` and `Meter` took `feedback.muted.border` (an edge used as a fill; then the quiet surface, a better borrow but still a borrow), and `Slider` took `input.primary.background.disabled` — **a _state_ used as a _part_**, so a Slider's empty rail meant "disabled" in the token model.
- **The reference does not borrow.** Spectrum 2 ships `track-color` as its own token, aliased to a private grey step rather than to any role's dimension, precisely because a rail's mode behaviour is its own: it **darkens** in dark (`rgb(218,218,218)` → `rgb(57,57,57)`) while every border in the system lightens. That is what a borrowed token cannot express — F-050's fix took the dark half from `muted.background` (`#3d3d3d`, four units off the reference) and paid for it in the light half (1.14:1 against the page, versus the reference's 1.40:1). No existing token carried **both** halves the rail wanted.
- **Resolved by option (a): a cross-cutting `semantic.rail.track`**, sibling of `focus`/`overlay`/`consequence` (model.md §6, fsl-theme ADR-028). Light lands on `core.colors.neutral.200` (`#e1e1e1`, 7 units off the reference's `rgb(218,218,218)` — closer than the borrowed `neutral.100` and closer than the next candidate step, `neutral.300`, at 10 units), raising the fill-vs-page separation from the borrow's 1.14:1 to ~1.31:1. Dark keeps `core.colors.neutral.700` (`#3d3d3d`) — the same value F-050 already found 4 units off the reference and correct; only the address needed to change there, not the pixel. `Slider`'s dark rail is the one that actually moves: `input.primary.background.disabled` (`neutral.900`) → `neutral.700`, which is the real defect this closes.
- **All three components now read the identical constant** (`RAIL_FILL` in `src/tokens/rail.ts`, renamed from `FEEDBACK_RAIL_FILL`), retiring both borrows. Guarded from both sides: fsl-theme's `rail.test.ts` pins the resolved values and that they no longer collapse onto either borrowed token; fsl-ui's `rail.test.tsx` pins that all three components read the same `var()` reference, not a resolved colour, so a refactor that reaches either old borrow by a different path still fails.
- **Not folded into F-050's fix at the time, and rightly so** — ADR-033 named this exact option and deferred it as "not urgent" once the blocker closed; the owner's delegation this round is what changed, not the underlying analysis, which ADR-033 and this entry both still hold.

### F-052 — a rail has no width envelope, so it collapses in a narrow cell and stretches past the reference's cap

- **Date:** 2026-08-05 · **Surface:** `@ttoss/fsl-ui` `ProgressBar`/`Meter`/`Slider` · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-06 (fsl-ui ADR-036) — owner ruling delegated this round
- Measured in Chromium at 390/640/900/1280/1920, both modes: the rail is declared `width: 100%` with no floor and no ceiling, and rendered **310 → 1840px**. The reference sets `progress-bar-minimum-width` / `meter-minimum-width` at **48px**, `*-maximum-width` at **768px**, and `meter-default-width` at 192px. So at 1280 and 1920 ours is 1.6× and 2.4× the reference's widest value, and at the bottom there is nothing stopping a rail in a narrow flex or grid cell from collapsing toward zero while the component still renders — the same shape as F-046, where `Dialog` had a ceiling and no floor.
- **The floor shipped at the reference's 48px** (`TRACK_RAIL.minWidth`, ADR-033), unaffected by this ruling: below it a rail stops carrying a readable proportion, and nothing wants that.
- **The ceiling ships as an opt-in host knob, not a hard cap.** `--fsl-track-max-width` (read via `fslVar`, ADR-031's `--fsl-dialog-min-width` precedent) defaults to `none` — every existing consumer's rail still fills its container exactly as before — and a host that wants the reference's 768px sets the property on `[data-scope]`. No measurement forced a default cap, and hard-capping every rail by default would be a breaking layout change for any consumer currently filling a wide container, the same reasoning that kept F-046's ceiling out of `Dialog`'s default.
- **`Meter`'s `meter-default-width` (192px) is not adopted.** Read from the flattened `@adobe/spectrum-tokens@14.15.0` data rather than assumed: `progress-bar-minimum-width`/`-maximum-width` exist and no `progress-bar-default-width` does; `meter-minimum-width`/`-maximum-width` **and** `meter-default-width`/`meter-width` (192px desktop / 240px mobile) all do. The reference itself draws the line: `ProgressBar` fills whatever length hosts it; `Meter`, a static gauge, additionally has an intrinsic size when unconstrained. Defaulting `Meter` to a fixed width would be the same breaking-change class the ceiling knob exists to avoid, and this package already gives a host a composition-level way to fix any block's width (`Box`/`Container`, CONTRACT §7.1) — a `Meter`-only default would duplicate that capability while breaking ADR-033's "one silhouette" ruling for the first time.
- **Guarded** in `tests/unit/tests/rail.test.tsx`: the knob is unset by default (every rail's `maxWidth` resolves through `var(--fsl-track-max-width, none)`, and `width: 100%` is unchanged), and all three components read the identical knob name so one host rule caps all three.

### F-053 — `Badge` and `StatusLight` are the same silhouette, distinguished only by which colour family they read

- **Date:** 2026-08-05 · **Surface:** `@ttoss/fsl-ui` `StatusLight`/`Badge` · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-05 (fsl-ui ADR-038) — owner ruling delegated this round
- Measured in Chromium (P3 round 3): `StatusLight` renders one part, a **filled pill** — 25.5×77.5 at 1280px, `padding: 3px 12px`, `borderRadius: 8px`, `background: feedback.{evaluation}.background` — sharing `CHIP_BOX` with `Badge`. The reference's `StatusLight` is a different object: a **dot plus a label on the page**, no fill (`status-light-dot-size-medium` **10px**, `status-light-text-to-visual-100` **6px**, `status-light-top-to-dot-medium` 11px).
- **This is the second half of F-040, which fixed the names and left the forms alone.** That entry established that our two chip names were inverted relative to both references, and the fix made `StatusLight` the valence-bearing one and `Badge` the descriptive one — sharing a box "while differing in what the colour is saying". So the differentiator is colour semantics only: a reader cannot tell an _observed state_ from a _category label_ by shape, and a `Badge` with a neutral palette beside a `StatusLight` with a neutral palette are indistinguishable.
- **The reference distinguishes them by form, and cheaply.** A dot is a 10px circle and a 6px gap; it also removes the fill, which is what makes a status read as ambient rather than as a control. Chakra agrees on the pairing (`Status` is a dot; `Badge` is the filled chip).
- **Resolved by adopting the reference's form.** `StatusLight` now renders two parts — `data-part="dot"` (a 10px circle, `radii.round`, filled from `feedback.{evaluation}.background.default`) and `data-part="label"` (the same `label.sm` type `CHIP_BOX` used, inked from `informational.primary.text.default` — the address `Surface`/`Code` already use for text on the page — not the feedback valence, because the reference publishes no `status-light-text` colour token; the dot alone carries the valence). `CHIP_BOX` import dropped entirely; `Badge` is now its sole reader. Geometry lives in `src/tokens/statusLight.ts`: the dot size and the dot-to-label gap are the reference's own desktop numbers (10px, 6px), and the reference's third number (`status-light-top-to-dot-medium`, 11px — a vertical-centring pad its own layout tool needs) is not carried forward: `align-items: center` on the inline-flex row centres the dot against the label's line box directly, verified in Chromium against our own label type (one step larger than the reference's, F-021).
- **What it costs, stated rather than hidden: a visible break on every existing `StatusLight` consumer.** The pill is gone; every `StatusLight` in the Studio (`DashboardPage`'s KPI deltas and deploy-status table cells, `BillingPage`'s plan status) and in Storybook now renders the dot+label form. None of those sites depended on the pill's fixed box for row alignment — they are inline flex rows with `align`/`gap`, which the new inline-flex shape fills the same way — so no consumer code changed, but every one of them **looks** different, which is the accepted cost of finishing F-040 rather than leaving "the names are right" to do all the work.
- **Guarded from both sides** in `StatusLight.test.tsx`: the dot resolves the evaluation's `feedback.*.background.default`, and the root carries none of `CHIP_BOX`'s chrome (`backgroundColor`, `borderWidth/Style/Color`, `paddingInline/Block` all empty) — verified to fail on injection when the old fill was added back onto the root. `Badge.test.tsx`'s box-sharing guard is inverted to assert the two roots now _disagree_ on that chrome.

### F-054 — `Toast` ships the reference's mobile cap at every viewport, and its inset contradicts ADR-031's own table

- **Date:** 2026-08-05 · **Surface:** `@ttoss/fsl-ui` `Toast`/`ToastRegion` · **Severity:** paper-cut · **Status:** resolved 2026-08-06
- Read from source in P3 round 3; the in-browser half is carried, because the surface renders from a queue interaction the measurement probe could not drive. Two separate things, filed together because both are one component's own literals:
- **(a) `TOAST_REGION_MAX_WIDTH` is `min(420px, calc(100vw - 2rem))`.** The reference sets `toast-maximum-width` at **336px desktop / 420px mobile**, so we ship the mobile value everywhere with no comment saying why. That is exactly F-047's shape one component over — and F-047 took the mobile value _deliberately_, with the reason written down (our label type is a step larger than the reference's). Here the same choice is undocumented, so it cannot be told apart from an oversight.
- **(b) The padding is `inset.surface.md`, and ADR-031's occluding table assigns `inset.surface.xs` to every occluding member except `Dialog`** — with `Toast` named in that table's member list. Either `Toast` frames prose the way `Dialog` does and the table owes a second exception, or it is a row-framer and the code owes the tighter step. The table and the code cannot both be right.
- **Resolved, in a real browser, both halves.** (a) Driving the `Show negative toast` story button and then forcing `ToastRegion` to 336px in Chromium wraps "Check the build log for details." from one line (27px) to two (54px) — our type ladder runs a step larger than the reference's (F-021/F-047), so 336px would cost a line the reference itself does not pay for. `420px` stays, uniformly, with that measurement written next to the constant. (b) `Toast` frames a title + description exactly the way `Dialog` frames prose — neither is a row-framer — so ADR-031's table now names both as the `md` exception (CONTRIBUTING.md ADR-031 addendum 2026-08-06). No code change; the table was the artefact that was wrong.

### F-055 — a Collection row's resting fill is the entity's own idiom, not the container's, so every row is a filled box in dark

- **Date:** 2026-08-06 · **Surface:** `@ttoss/fsl-ui` `GridListItem`/`ListBoxItem`/`TableRow` × `@ttoss/fsl-theme` `input.primary.background` / `informational.primary.background` · **Severity:** paper-cut · **Status:** resolved 2026-08-06
- P3 review round 4 (Collection: `GridList`, `ListBox`, `Table`). All three Selection-entity row implementations call `resolveInteractiveStyle(c?.background, flags)` where `c = vars.colors.input.primary` — with every flag `false` at rest, that returns `input.primary.background.default` unconditionally, whether the row is at rest, hovered or selected.
- Measured in Chromium, both modes, at 1280px: in light, `input.primary.background.default` is `core.colors.neutral.0`, byte-identical to the container's own `informational.primary.background.default` — the row reads flush against its container, which is correct, and is correct by luck. In dark, the alternate remaps `input.primary.background.default` to `core.colors.neutral.700` (a text-field's filled-box look) while the container stays `neutral.900` — every row in `GridList`/`ListBox`/`Table` rendered a solid `rgb(61,61,61)` block on a `rgb(22,22,22)` container, at rest, with no hover or selection. The reference has no "row background, resting" token at all (`table-selected-row-background-*` and `table-row-hover-color`/`-opacity` exist; nothing for the unselected, unhovered state) — its rows are transparent at rest by construction, tinted only on interaction.
- `families/colors.md`'s idiom for "no fill" is `muted`: "the surface's own colour … no visible edge at rest, the fill appears on hover." `MenuItem` had the identical defect and ADR-015 closed it by switching its default `evaluation` to `muted`, because `action.muted.background.default` happens to equal the popover's own fill in both modes. `Selection` has no `evaluation` to swap (`ENTITY_EVALUATION.Selection = []`), and no `input.*` role's `default` matches `informational.primary.background`'s dark value either — the model has no address for "the container's own colour, read from inside an item that belongs to a different entity" (F-051's shape, one family over).
- **Fixed by composition, not a new token.** `resolveCollectionRowBackground` (`src/tokens/collectionRow.ts`) overrides only the `default` key of the item's own colour states with the container's already-resolved background before running the state cascade — hover/active/selected/checked keep exactly the values they had. The container's background is already in scope at every call site (the root component reads the identical token to publish its own surface), so nothing new was registered. Guarded from both sides in `tests/unit/tests/collectionRow.test.tsx`: the resting read matches the container, and the entity's own `default` is asserted **not** reached.
- Verified in Chromium: `Table`'s dark row fill went from `rgb(61,61,61)` to `rgb(22,22,22)` (matching its `rgb(22,22,22)` container); `GridList`/`ListBox` the same. Selection (`checked`) and hover fills are unchanged — screenshotted light and dark, both still show the expected `checked` white pill and hover tint.

### F-056 — `Disclosure` and `Accordion` restated one design decision under two names

- **Date:** 2026-08-06 · **Surface:** `@ttoss/fsl-ui` `Disclosure`/`DisclosureTrigger`/`DisclosurePanel` and `Accordion`/`AccordionTrigger`/`AccordionPanel` · **Severity:** paper-cut · **Status:** resolved 2026-08-06
- P3 review round 6 (Disclosure). `Disclosure` (one section) and `Accordion` (a `DisclosureGroup` of `AccordionItem`s) are two composites over the same `Disclosure` entity, and their per-part implementations were copies under two names: `DisclosureTrigger`/`AccordionTrigger`'s style builder was byte-for-byte identical (box, padding, state cascade, transition, chevron), as was `DisclosurePanel`/`AccordionPanel`'s inner-wrapper style and both roots' container chrome (border width/style/colour/radius/`overflow: hidden`).
- Not a colour or geometry defect — both composites already agreed, correctly, on every value. The defect is that the agreement was restated four times rather than declared once: a change to the trigger's padding or the panel's `publishSurface` call would have had to land in both files identically, which is exactly the class Slices 3/4/5 removed elsewhere (`selectionControl.ts`, `chipBox.ts`, `rail.ts`, `Field/anatomy.tsx`) and the class this round's instructions named directly.
- **Fixed by extraction.** `src/tokens/disclosureAnatomy.ts` — the Disclosure-family counterpart of `ActionTrigger/anatomy.tsx` and `Field/anatomy.tsx` — exports `buildDisclosureContainerStyle`, `buildDisclosureTriggerStyle`, `buildDisclosureIndicatorStyle` and `buildDisclosurePanelBodyStyle`. Both composites now call the same four builders; `data-scope`/`data-part` stay per-file (CONTRACT §5: sub-parts reuse the _host's_ scope, and the host differs). Guarded directly in `tests/unit/tests/disclosureAnatomy.test.tsx` (the module's own contract, not only through a component), and verified pixel-identical in Chromium, both modes, before/after screenshots of `Accordion` and an expanded `Disclosure`.

### F-057 — `Surface`'s boundary has no address of its own at an elevated tonal stratum, and in dark it collides byte-for-byte with the fill

- **Date:** 2026-08-06 · **Surface:** `@ttoss/fsl-ui` `Surface` × `@ttoss/fsl-theme` `semantic.elevation.tonal.{overlay,blocking}` / `semantic.colors.informational.{primary,secondary,muted}.border.default` · **Severity:** paper-cut · **Status:** ✅ resolved 2026-08-06, as a side effect of F-048 (fsl-ui ADR-037)
- P3 review round 7 (Structure). `Surface`'s edge is a fixed read — `vars.colors.informational[evaluation].border.default` — independent of `level`; its fill is `vars.elevation.tonal[level]`. The two are unrelated tokens from unrelated families, so nothing declares that they must differ, and in the dark alternate they do not: `overlay` and `blocking` both resolve `elevation.tonal` to `core.colors.neutral.700`, which is **also** the dark value of `informational.primary.border.default`, `informational.secondary.border.default` **and** `informational.muted.border.default` — every evaluation this component can take. Measured in Chromium at 1280px, `structure-surface--levels`, dark: `level="overlay"` and `level="blocking"` both render `border-color: rgb(61,61,61)` on `background-color: rgb(61,61,61)` — **1.00:1, no edge at all** — while `level="raised"` (fill `neutral.800`) and `level="flat"` (fill `neutral.900`) render a visible edge against the same fixed border. In light, all levels share `neutral.0` for the fill by design (elevation is carried by shadow there, per `baseTheme.ts`'s own comment), so the collision is dark-only and was invisible from that side.
- **Why no suite saw it, and it is the fourth instance of the same asymmetry.** `extractBorderBackgroundPairs` (F-027/ADR-024) pairs a border against **its own `ux.role`'s** background — `informational.muted.border` against `informational.muted.background`, never against `elevation.tonal`, a different family entirely. The defect lives in what `Surface` actually composes, which is exactly the class F-044 (border vs. cross-stratum page), F-050 (fill vs. cross-role rail) and F-055 (row vs. container) each named: the inventories are organised by token **kind**, not by what a component paints next to what.
- **Related to F-048, not a restatement of it.** F-048 asks what `evaluation` should mean when a _different_ family of components (the overlays) adopts `tonal` at all — a governance question about a prop's meaning on components that do not read `tonal` today. This entry is narrower and does not wait on that answer: `Surface` already reads both `tonal` and `border` unconditionally, and the two already collide for its own existing consumers.
- **A repair was attempted and reverted, and the reason is itself evidence.** The seemingly obvious fix — give the dark ramp three distinct, _lighter_ steps instead of two (`raised: 800, overlay: 600, blocking: 500`, continuing the "each rising stratum steps toward a lighter neutral" direction the theme's own comment already states) — passes `elevation.test.ts`'s monotonic-ramp guard and removes the identity collision, but **breaks three unrelated cross-role legibility pairings** (`tests/unit/tests/theme/families/colors.test.ts`: "validation message", "focus ring", "quiet destructive control" are legible on every informational stratum): those inks are red/pink values tuned against `neutral.700` (contrast ≥4.5:1 there), and a _lighter_ fill lowers their contrast — measured **4.12:1 against the `neutral.600` overlay step and 2.65:1 against `neutral.500` blocking**, both under the 4.5:1 floor those pairings assert, in **both** bundles (`bruttal` inherits `tonal` from the base bundle, having none of its own). Reverted rather than shipped partial: lightening the tonal ramp to clear the border collision costs the ink floor the surface-publication contract (ADR-030) already guarantees on every tonal stratum.
- **The two contracts pull in opposite directions, which is the actual finding.** A boundary that must differ from a _rising_ (lightening) fill wants to move further from `neutral.700` in the light direction; ink that must stay legible against that same fill wants it to stay dark. No single step of the existing 100-unit neutral ramp satisfies both at `overlay` and `blocking` simultaneously, because `informational.*.border.default` is fixed at exactly the step the ink contrast was tuned against. This is the same shape F-051 named for the rail — "the model has no address for X, so the component borrows a token whose meaning is something else" — one family over: `Surface`'s elevated boundary borrows the _flat card's_ edge token, which was never designed to track a rising tonal fill.
- **Costed options**, none built: **(a)** a cross-cutting `semantic.elevation.edge.*` (sibling of `tonal`, one step per stratum, engineered independently of both the informational border ramp and the validation/focus/destructive ink floors) — the address F-051 recommended for the rail, same shape, same version-boundary timing. **(b)** Keep one boundary value but move it _darker_ than `neutral.700` instead of lighter — e.g. reuse `informational.primary.background.default` (dark `neutral.900`) as the elevated boundary — which clears both floors (darker than every fill it borders, and unaffected by the ink pairings since it is not itself a fill token) but repurposes a _background_ token as a _border_, the same category mismatch F-051 flagged for `Slider`'s state-as-part borrow. **(c)** Leave it: `Surface`'s edge is documented as "the quiet default that lets elevation carry the separation" (its own JSDoc) and `overlay`/`blocking` already carry a visible `box-shadow` in dark (`elevation.surface.overlay/blocking`), so the missing hairline is redundant with a depth cue that is present — cost is that `forced-colors`/shadow-suppressed contexts (the exact case ADR-031 wrote the occluding-boundary duty for) are left with no edge at these two levels, same failure mode as F-044 before its fix, on a component ADR-031 explicitly classified as **not** owing that duty.
- **Recommendation: (a), at the version boundary**, for the reason F-051 gave verbatim: nothing about it is urgent (the shadow gives `overlay`/`blocking` a depth cue in the interim, per (c)), and what was missing was the analysis, now on record with both a failed repair and the numbers behind why it failed.
- **Resolved 2026-08-06, superseded by F-048's fix rather than by any of the costed options above.** The owner's delegation this round covered both findings together, and F-048's ruling (`Surface` reads `informational.{evaluation}.background.default` for fill, not `elevation.tonal[level]` — fsl-ui ADR-037) removes the actual mechanism this entry names: the border was never colliding with "the tonal ramp" as such, it was colliding with _whatever family the fill happened to read_, and once that family is the border's own (`informational`), the pair falls under `colors.test.ts`'s existing `extractBorderBackgroundPairs` audit for every other `informational` role.
- **Measured, Chromium, `structure-surface--evaluations`, dark, both evaluations, `overlay`/`blocking`** (identical at both levels — the fill and border are level-independent within a mode): `evaluation="muted"` — fill `#6f6f6f`, border `#3d3d3d`, **2.16:1** (was 1.00:1). `evaluation="primary"` — fill `#161616`, border `#3d3d3d`, **1.67:1** (was 1.00:1). Neither clears the 3:1 an _occluding_ boundary owes — but `Surface` is embedded, not occluding (ADR-031), so it never owed that floor; what this entry actually named was the mirrored 1.00:1, and that is gone in both evaluations. Light, for completeness: 1.15:1 (`muted`) / 1.31:1 (`primary`), both already non-mirrored before and after.
- **Neither costed option was built.** (a) (`semantic.elevation.edge.*`) and (b) (borrow a background token as the border) both assumed the fill would keep reading `tonal` and the border would need to move to clear it. With the fill's own family changed instead, the border needed no change at all — it already reads `informational.{evaluation}.border.default`, unaffected by this fix, and is now paired against the same role's own `background`, which is what the theme's border-pairing suite is built to audit. **Option (c) — "leave it, the shadow carries depth in the interim" — is the one that held**, not because nothing moved, but because the fix that landed was orthogonal to all three: `Surface` still keeps its embedded edge and its shadow; only its _fill's provenance_ changed.

---

### F-058 — `Toast`'s trailing optical inset has no token step that lands it symmetric

- **Date:** 2026-08-06 · **Surface:** `@ttoss/fsl-ui` `Toast` × `@ttoss/fsl-theme` `semantic.spacing.inset.surface` · **Severity:** paper-cut · **Status:** open
- Found while giving `Toast` the reference system's anatomy (ADR-040). The reference halves its trailing inset — `paddingStart: 16` / `paddingEnd: 8` — because the close trigger's own box already supplies breathing room on that edge. The effect is real here too, and was measured in Chromium at 1280px on `feedback-toast--lengths`: the leading edge sits **37px** from the valence glyph's ink, the trailing edge **41.7px** from the close trigger's ink. The trailing side reads **4.7px heavy**.
- **The obvious repair was built, measured, and reverted.** Stepping `paddingInlineEnd` down to `inset.surface.sm` (24px against `md`'s 36px) puts the close ink at **29.7px** — now **7.3px light**, a worse error in the other direction. The correction actually owed is half the trigger's internal inset (~4.7px), and the `inset.surface` ladder has no half-step between `sm` and `md`. So the uniform `md` stays, a unit test now asserts the uniformity, and the residual is filed here rather than papered over with a named literal — which CONTRACT §4 would make a component-level decision about a value the theme owns.
- **Costed options**, none built: **(a)** an `inset.surface` half-step between `sm` and `md` — smallest change, but grows a ladder used by every surface in the system to fix one component's trailing edge, and model.md §6's reuse-before-creation gate would want more than one consumer. **(b)** Shrink the close trigger's box so its internal inset stops over-supplying — rejected on sight: the box is `sizing.icon.lg` (29.36px measured) and WCAG 2.5.8's 24×24 floor argues for keeping it, not trimming it. **(c)** Leave it: 4.7px on a 420px surface is under the threshold at which the asymmetry reads without a ruler, and the trailing side being the _heavier_ one is the forgiving direction — a cramped close button would be the noticeable failure. **(d)** A negative inline-end margin on the trigger to absorb its own inset — arithmetically exact and the worst of the four, since it makes a hit target overhang its container's padding box.
- **Recommendation: (c), until a second consumer appears.** This is the same shape as F-047/F-054 — a residual that is real, measured, and smaller than the cost of the mechanism that would remove it. If a second component lands the same complaint (any surface with a trailing icon trigger inside a `surface` inset), that is (a)'s second consumer and the gate opens.

### F-059 — A control inside a Feedback surface has no pointer affordance, because the context has no hover state to resolve

- **Date:** 2026-08-06 · **Surface:** `@ttoss/fsl-ui` `Toast` × `@ttoss/fsl-theme` `semantic.colors.feedback` · **Severity:** paper-cut · **Status:** open
- `FeedbackColorStates` admits `default | focused | disabled` and no more — FSL §7, on the correct premise that feedback surfaces are not interactive triggers. But a toast hosts two controls that _are_: the close trigger (shipped since the component landed) and now the action trigger (ADR-040). Both paint from `feedback.{evaluation}` because the entity → ux-context contract test binds this file's colour reads to the entity it declares, and that is also the right visual answer — an `action.*` control would arrive with the page's palette on top of a saturated red. The consequence is that `resolveInteractiveStyle` returns the **resting value at every pointer state**: hovering the close button or the action changes nothing. Keyboard users are served (the focus ring is a cross-cutting token and works), pointer users get no feedback at all.
- This has been true since `Toast` shipped and went unnoticed because the close trigger is a glyph on a fill, where "no change on hover" reads as flat rather than as broken. The action trigger makes it legible: an outline button that does not respond to the pointer looks disabled.
- **Not worked around in the component**, deliberately. Every available in-component fix is a category violation: reading `action.muted` breaks the contract test; reading `feedback.muted` (`neutral.100`) puts a light chip on a saturated fill and means a different role; a hardcoded `rgba()` overlay is the F-024 shape the package has already ruled against twice, an untokenised paint no contrast audit can see.
- **Costed options**, none built: **(a)** admit `hover`/`active` into `FeedbackColorStates` for the roles that ship a fill, engineered against the same `neutral.0` ink each fill already pairs with — the honest fix, and a `packages/fsl-theme` change with the contrast suite's pair extractors to satisfy for four new fills × two modes. **(b)** A cross-cutting `semantic.overlay.wash.*` — a tint a control paints _over_ its host's fill regardless of family, sibling of `scrim` and `overlay.outline`, which is the shape F-053's boundary problem resolved into and would serve any "control on someone else's voiced surface" (a Badge with a remove affordance, a filled `Banner`'s dismiss). Wider blast radius, one new address, and it does not multiply per valence. **(c)** Leave it: the resting-only cascade is a documented FSL §7 consequence, not a defect, and both triggers are secondary to a surface whose job is to be read rather than operated.
- **Recommendation: (b) at the version boundary**, for the reason F-051/F-057 gave for the same shape of question — the need crosses `ux` (a toast is `feedback`, a filled badge is `informational`), so the per-context grammar cannot express it, and §6's cross-cutting gate is exactly the test it passes. (a) is the smaller change but pins the fix to one context and multiplies the audit surface by valence. Nothing here is urgent: no flow is blocked, and the missing cue is on the _quiet_ member of a surface that already demands attention on its own.

### F-060 — The browser's own in-field controls draw into a frame the field anatomy already furnished

- **Date:** 2026-08-06 · **Surface:** `@ttoss/fsl-ui` `Field/anatomy` (`buildFieldValueStyle`) · **Severity:** gap · **Status:** ✅ resolved 2026-08-06 (fsl-ui ADR-041)
- Reported from a product screen: a `SearchField` with text shows **two ✕** in the trailing corner. Reproduced in Chromium at the first attempt — one is the clear button the anatomy draws, the other is `::-webkit-search-cancel-button`, painted by the browser in its own colour because the input is `type="search"` (React Aria's `useSearchField` sets it, correctly — the type carries the Escape-clears behaviour and the search semantics).
- **Root cause is the anatomy's, not `SearchField`'s.** A field here is a _frame that owns its interior_: the leading glyph, the invalid mark, the clear button and the steppers are all placed inside it by shared modules. The `<input>` inside that frame also brings controls of its own, decided by its `type` and drawn into the same box, and nothing in the anatomy reconciled the two. Wherever a UA control and an anatomy adornment mean the same thing, both render.
- **Measured, before deciding the blast radius.** Read from the rendered DOM in Chromium: `search-field` is the only control in the package carrying a decorated type (`type="search"`); `NumberField` is `type="text"` with `inputmode="numeric"` (React Aria supplies the spinbutton semantics, so the native steppers never appear), `TextField` and `ComboBox` are `type="text"`. So one component is affected today — but `TextField` passes a caller's `type` straight to the input, which puts the same defect one prop away, and `type="date"` was confirmed to render `::-webkit-calendar-picker-indicator` inside our frame right now.
- **`appearance: none` on the input does not fix it** — tried first, since it would have been an inline one-liner in `buildFieldValueStyle` and needed no new mechanism. Screenshotted: current Chromium keeps the cancel button. Only the pseudo-element rule removes it, and a pseudo-element cannot be addressed from a `style` object, which is the only styling channel this package uses.
- **Resolved at the anatomy, type-agnostically.** New `src/tokens/nativeFieldDecorations.ts` injects one `<style>` per document via the `ensureKeyframes()` pattern, suppressing exactly the decorations that duplicate an adornment the anatomy draws (`search-cancel-button`, `search-decoration`, `search-results-button`) and deliberately leaving every decoration that is the _only_ affordance for its type (calendar indicator, spin buttons, password reveal) to the browser. It is called from `buildFieldValueStyle` — the one place every field control passes through — rather than from a component, because a per-component call is one more thing to forget on the next field, which is how this shipped. Verified in Chromium: one ✕, ours.

### F-061 — The adaptive ButtonGroup's overflow verdict goes stale when a child resizes after mount

- **Date:** 2026-08-06 · **Surface:** `@ttoss/fsl-ui` `ButtonGroup` (`useAdaptiveColumn`) · **Severity:** paper-cut · **Status:** open
- Found by the E2 wave-1 pixel-parity gate, which is exactly the kind of eye that catches it: in the `collapses-when-it-does-not-fit` story, the light capture rendered the 360px "fits" case **collapsed** while the dark capture of the same page session — same code, later in the load — rendered it correctly side by side. Two independent fresh-server captures reproduced the split; the ADR-013 suite and the dark frame prove the verdict logic itself is intact.
- **Root cause:** the verdict is measured once per `children` identity in a mount-time layout effect, and invalidation watches only the **container** (`ResizeObserver` on `parentElement`). Inter Variable arrives after first layout; the fallback-font row measures wider than the container, the group settles on `isColumn: true`, and when the real font lands narrower nothing re-measures — a child's resize is invisible to the container observer. The race runs in both directions (wrongly collapsed row, or an overflowing row that should have collapsed), lasts until the next container resize, and any consumer on a slow font path can ship it to users.
- Not introduced by E2 — the extraction changed module-load timing enough to lose the race in the probe, which is how a pre-existing hole became visible. The capture harness now documents the one legitimate non-noise diff it has produced.
- **Costed options**, none built: **(a)** observe the children too — correct signal, but settling _changes_ the children's layout, so the observer must not re-invalidate on the resize the settle itself causes; the verdict-key machine absorbs one cycle but an oscillating fit (row fits ⇄ column fits) could ping-pong. **(b)** `document.fonts.ready.then(invalidate)` — one-shot, targets the actual late resizer, no loop surface; misses non-font child resizes (an icon loading, a label swap), which today have no known consumer. **(c)** leave it: transient, self-heals on any container resize.
- **Recommendation: (b)** — the only late child resize with a real consumer is the webfont, and `fonts.ready` is its precise, once-per-document signal. Re-open toward (a) if a non-font consumer appears.

### F-062 — the reference's InlineAlert was read as tinted; tinted is its opt-in, and the default is a bordered neutral

- **Date:** 2026-08-12 · **Surface:** `packages/fsl-ui/INTERNAL/ROADMAP.md` (B3b) + `INTERNAL/FORMS.md` (standing deferrals) vs `@react-spectrum/s2` `InlineAlert` · **Severity:** paper-cut (doc) · **Status:** ✅ resolved 2026-08-12, same day
- Found while scoping `InlineAlert` (the `status.passive` counterpart of `Toast`). Two planning docs carry the same sentence, verbatim, as the reason `FormErrorSummary` is parked: _"`feedback.negative` is a filled rung while the reference's InlineAlert is **tinted** and the tinted rung lives in `informational.negative`, which §1 forbids a Feedback component from reading."_
- **Read from the reference's source, not from a screenshot:** `packages/@react-spectrum/s2/src/InlineAlert.tsx` takes `fillStyle?: 'border' | 'subtleFill' | 'boldFill'` and **defaults to `'border'`** — a neutral `gray-25` surface with a valence border and a valence icon. `subtleFill` (`{valence}-subtle`) and `boldFill` (`{valence}`) are opt-in. So the sentence describes the _second_ of three styles as though it were the component. Spectrum v3's `InlineAlert` has no `fillStyle` at all and is the same bordered shape.
- **Why the misreading mattered.** It made the blocker look like a missing _tinted fill family_ (4 roles × 3 dimensions × 2 modes) when the default style needs no valence fill at all — only a valence **ink** and a valence **edge**. The cheaper diagnosis was one prop signature away the whole time.
- **What the reference's default costs us that a screenshot hides:** `fillStyle` is a variant axis, and CONTRACT §4 forbids one (no `size`, no `variant`). fsl-ui must pick one style and keep it; it cannot ship the reference's three.
- **Corrected in both docs**, and the deferral's reason restated: `FormErrorSummary` waits on its consumer (a form tall enough to scroll the first invalid field off-screen), not on a tinted fill family. The colour half is answered by `semantic.valence.{positive,caution,negative}.ink` (fsl-theme ADR-029).
- **Lesson, and it is the CLAUDE.md rule one layer out.** "Read the design documentation before deciding anything" has a sibling: read the _reference's_ source before quoting its behaviour. This sentence was copied between two planning docs and cited in three rounds of analysis without anyone opening the file it describes — the same shape as F-032, where the reasoning was sound and the reading was missing.

### F-063 — a `secondary` action inside a quiet Feedback surface resolves the surface's own colour in dark, on fill and edge alike

- **Date:** 2026-08-12 · **Surface:** `@ttoss/fsl-theme` `semantic.colors.action.secondary` × `semantic.colors.feedback.muted.background` · **Severity:** gap · **Status:** open
- Found while designing `InlineAlert`, whose surface is the quiet Feedback rung (`feedback.muted`) and which hosts caller-supplied actions as children — the composition that lets it host real `action.*` controls at all, since a quiet neutral surface is exactly where the page's palette is correct (the inverse of the argument `Toast` makes for re-dressing its own triggers, ADR-040).
- **Measured from the resolved theme, both bundles, before deciding anything.** Dark: `feedback.muted.background.default` is `#3d3d3d`; `action.secondary.background.default` is `#3d3d3d` and `action.secondary.border.default` is `#3d3d3d` too (a mirrored edge, by design — it is a listed member of the border pairing's `MIRRORED_BORDERS` set). **1.00:1 on fill and 1.00:1 on edge**: a secondary button inside the surface disappears as an object and only its `#f8f8f8` label remains. `bruttal` inherits every one of these values, so it reproduces identically.
- **Separation of every action rung against this surface**, so the guidance is measured rather than asserted:

  | Rung        | Dark (on `#3d3d3d`)   | Light (on `#f0f0f0`) |
  | ----------- | --------------------- | -------------------- |
  | `primary`   | **10.86:1**           | **17.37:1**          |
  | `accent`    | 2.26:1                | 4.22:1               |
  | `secondary` | **1.00:1** (mirrored) | 1.15:1               |
  | `muted`     | 1.67:1                | 1.14:1               |

- **Two of those four rows are not this entry's finding.** `muted` at 1.67:1 is the F-024 class, whose ruling already accepted it as a known limit of the quiet rung with `publishSurface` named as the lawful fix. And `secondary`'s light 1.15:1 is not a defect either: that rung is "a light fill, visibly present, clearly below primary" by design and never owed a 3:1 boundary — it reads about the same against the page. What is new is the **dark identity**: not "low separation" but the _same value_, on both dimensions, which is the mirrored-collision class the theme treats as a defect elsewhere.
- **Why no suite saw it, and it is the fifth instance of the same asymmetry.** `colors.test.ts`'s "roles within a context are distinguishable" invariant compares roles _inside_ one `ux`; this pair is `action` against `feedback`. The border extractor pairs a border against **its own role's** background. Neither can look across contexts — the same organised-by-token-kind blind spot F-044, F-050, F-055 and F-057 each named, now between a control and the surface a _sibling component_ paints under it.
- **Costed options**, none built: **(a)** admit `feedback.muted` to the §3.4 publisher set — narrowing "Feedback fills are voices" to "Feedback **valence** fills are voices", which is true by construction since `muted` is the entity's no-fill rung and a stratum rather than a voice. Then a quiet action inside the surface borrows the published fill and materialises correctly on hover, which also unblocks an in-surface dismiss trigger. Costs one cross-role inventory row (the quiet ink and the destructive ink against `feedback.muted`, both modes, both bundles) and it does **not** fix `secondary`, which is a voiced fill and never follows a surface. **(b)** Document the rule instead: an action inside a `status.passive` surface is `primary`. Zero cost, and it is defensible on its own terms rather than as a workaround — the reference ships no actions at all, `Toast` allows exactly one, and "the primary path out of this condition" is a semantic claim, not a colour dodge. Its cost is a footgun: a caller passing `secondary` gets a silent dark-only failure, which is the exact class F-024 kept rediscovering. **(c)** Give `action.secondary` a dark fill that is not `neutral.700` — smallest-looking and worst: that step was chosen in F-025 to fix `secondary`/`muted` collapsing onto each other, and moving it re-opens a settled ladder to fix one host surface.
- **Recommendation: (b) now, (a) at the version boundary.** (b) is what `InlineAlert` ships with, stated in its JSDoc and its story rather than left to the caller to discover; (a) is worth doing when the dismiss trigger gets a consumer, because both needs are the same publisher change and it is cheaper to measure once. Not urgent under (b): the shipped default is correct in both modes, and nothing is unreachable.
