/**
 * Component Model — public entry point.
 *
 * Import from '@ttoss/ui2/model' for types, resolution functions,
 * and CSS custom property utilities.
 *
 * @example
 * ```ts
 * import { resolveTokens, colorVar } from '@ttoss/ui2/model';
 * ```
 */
export * from './_model/index';
